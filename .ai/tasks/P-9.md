# P-9

Artifact Handoff Between Implementer and Independent Reviewer（Implementer と独立 Reviewer の成果物受け渡し）

Status: CHANGES_REQUIRED

Analysis: `.ai/reports/P-9-analysis.md`（branch `claude/artifact-handoff-protocol-4gfgjg` @ `9044dcca87249a392258801dfcd7650d9a978f9f`）
Protocol baseline: `claude/chapter-structure-scenario-test-q6oj4l` @ `4d7662f20b8bd8da0a996c31c1a2c26644d63cee`（本計画の `AGENTS.md` 等の節番号・行番号はすべてこのコミット時点）

本 Task は Work Item を持たない Task（`AGENTS.md` §7.1）。承認単位は Task 全体。
本 Task は Protocol（`.ai/` 配下の Markdown）だけを対象とし、ゲームコード・テストは対象外。

## Request

MAGE-002 で、別セッション・別コンテナの Independent Reviewer が Implementer の未 commit 成果物を取得できず、
実装内容と無関係な BLOCKER / MAJOR を出した。Reviewer の report も Reviewer コンテナにしか無い状態が生じた。
これを防ぐ最小の Protocol 変更を計画する（Planner のみ。Protocol は変更しない）。

## Objective

Independent Reviewer が **決定的（branch + SHA で一意）かつ永続的（remote に存在する）なレビュー対象** を受け取り、
Review の結果も同じく永続的に残る状態にする。新しい Status・Agent・テスト枠組みは作らない。

## Problem

| 区分 | 内容 | 根拠（P-9-analysis） |
| --- | --- | --- |
| Implementation persistence | `TESTING → REVIEWING` の条件が「テスト全て成功」だけで、Reviewer が成果物を取得できるかを含まない | FACT 5 / 6 / 8、I-3 |
| Review target identity | Reviewer 入力は `git diff` とだけあり、branch / SHA / 差分範囲が定義されていない | FACT 3 / 12、I-4 |
| Push authority | Protocol に commit / push の規定が無く、Approval Scope の「自動 commit・push はしない」がそのまま handoff 禁止として働いた | FACT 2 / 10、I-5、U-1 |
| Review persistence | Reviewer は READ ONLY と定義され、report / Status の commit・push が規定されていない | FACT 9 / 13 / 14、I-6 / I-7 |

## Evidence

| # | 事実 | 根拠 |
| --- | --- | --- |
| EV-1 | Implementer は未 commit のまま `TESTING → REVIEWING` にした | `MAGE-002.md` Status History（@4d7662f）「Reviewer 待ち。未コミット」 |
| EV-2 | MAGE-002 の Approval Scope に「自動 commit・push はしない」 | `MAGE-002.md` Human Approval（@4d7662f） |
| EV-3 | 別セッション Reviewer は F-01 BLOCKER（成果物不在）/ F-02 MAJOR（Status・承認記録を確認できない）を出し、原因は受け渡しで実装不備ではないと記録した | `MAGE-002-review.md` Previous Findings |
| EV-4 | Implementation commit `88fb6fc924bb5f7120401480a3874d45efd3c08b`（spec / Task / analysis の3ファイル） | P-9-analysis E-7 |
| EV-5 | 再レビューは Branch・Commit・差分 `88fb6fc^..88fb6fc` を Reviewer が自主的に明記して行われ PASS | `MAGE-002-review.md` Review Scope |
| EV-6 | Review commit `4d7662f20b8bd8da0a996c31c1a2c26644d63cee` は `88fb6fc` の子で、差分は `.ai/reports/MAGE-002-review.md` のみ | P-9-analysis E-9 / FACT 18 |
| EV-7 | Review 後も `MAGE-002.md` は `Status: REVIEWING` のまま。T-1（兼務）では Reviewer が DONE を記録 | P-9-analysis E-11 / FACT 14 |
| EV-8 | T-1 Retrospective F-13「検証前に push しない」、DC-1「Reviewer 別セッション必須化」は未決 | `AI-AGENT-PROTOCOL-T1-retrospective.md` |
| EV-9 | 現行 Protocol は `claude/chapter-structure-scenario-test-q6oj4l` にだけ存在し、`main` には無い | P-9-analysis FACT 1 |

## Scope

変更対象（承認後、D-4 で決めたブランチ上でのみ）:

| ファイル | 節 |
| --- | --- |
| `.ai/AGENTS.md` | §4（「残す」の定義）、§5（Review Handoff の定義と Reviewer の書き込み範囲）、§6（Approval の Persistence 行）、§7 状態表 TESTING / REVIEWING 行 |
| `.ai/agents/implementer.md` | Procedure 手順4、Output Template（Review Handoff） |
| `.ai/agents/reviewer.md` | Permission / Input、Review Order の先頭（対象検証）、Output Template（Review Target） |
| `.ai/tasks/README.md` | Task Template / Work Item の Human Approval 欄に Persistence 行を1行 |

変更しない: `.ai/agents/analyzer.md` / `planner.md` / `debugger.md`、`.ai/reports/README.md`、`.ai/decisions/`、既存 Task・既存 report、`src/`、`tests/`、`basefile.html`、`docs/`、Playwright 設定。

## Proposed Protocol Change

要約（採用案 = Option A。D-1 で確定）:

1. **Implementation persistence gate**: `TESTING → REVIEWING` の条件に「承認範囲の成果物を作業ブランチへ commit・push し、Review Handoff を提示した」を追加する
2. **Review Handoff**: Reviewer への入力を branch + SHA + 差分範囲で固定する（§5 に1か所だけ定義し、agents 側は参照）
3. **Target verification**: Reviewer はレビュー前に SHA の取得可否と成果物の存在を確認し、確認できなければレビューしない
4. **Review persistence gate**: `REVIEWING → DONE` の条件に「review report と Status 更新が push 済み」「Reviewed SHA が最終実装 SHA と一致」を追加する
5. **Push authority の明示**: push は Human Approval 欄に明記された場合だけ許可される。暗黙の許可は無い

### Commit / push option の評価

| 案 | 信頼性 | 再現性 | 既存ワークフローとの整合 | 評価 |
| --- | --- | --- | --- | --- |
| **A** commit + push を REVIEWING 前に必須 | remote 実行環境・別の人間・同一セッションすべてで成立 | SHA で対象固定。後から第三者が同じ対象を取得可能 | §4「残してから渡す」§15「GitHub 上で受け渡す」の具体化。F-13「検証前に push しない」とも両立（push はテスト後） | **推奨** |
| **B** commit のみ、共有 repo なら push 不要 | 現行の実行環境（コンテナ非共有、FACT 17）では MAGE-002 がそのまま再発 | commit があれば SHA 固定は可能だが remote に無い | 「共有か」の判定を Agent に委ねる | 不採用 |
| **C** Reviewer が独立な時だけ push | 同一セッション兼務では remote に何も残らず、後から Review 対象を検証できない | 兼務時は不可 | 「独立か」の判定分岐が増え、§5 の兼務 Review（人間の差分確認を推奨）で人間が差分を取得できない | 不採用 |
| **D** A を基本とし兼務時は commit のみ | C と同じ欠点が兼務時に残る。規則が長くなる | 兼務時は不可 | 例外規定が必要 | 不採用（D-1 で人間が選ぶ場合の代替案として残す） |

A の追加コストは兼務時の push 1回のみ。push 先は作業ブランチであり `main` ではない（§6 の承認範囲外へ出ない）。

## State Transition

**新しい Status は作らない。** 永続化は既存遷移の **条件（gate）** として表す。

```
IMPLEMENTING → TESTING → REVIEWING → DONE          （状態列は現行どおり）
                        ▲ gate P1     ▲ gate P2
```

| 遷移 | 現行条件（§7） | 追加条件 |
| --- | --- | --- |
| `TESTING → REVIEWING` | 全て成功 | **P1**: 承認範囲の成果物（コード・テスト・Task の Implementation Result と Status 更新・その承認単位の analysis）を1つの commit 群として作業ブランチへ push し、Review Handoff を提示した |
| `REVIEWING → DONE` | PASS | **P2**: DONE Gate（下記）をすべて満たす |

P1 を満たせない場合（Persistence 不許可、権限、ネットワーク）:

- `REVIEWING` にしない。`TESTING` のまま止まり、理由を人間へ報告する
- 人間の判断待ちが続く場合は既存の `BLOCKED`（理由: Review Handoff 未完了）を使う（§7「任意の状態 → BLOCKED」）

「IMPLEMENTATION PERSISTED」「REVIEW PERSISTED」を Status にしない理由: どちらも次の遷移の前提条件であり、
独立した作業状態（担当者が作業している状態）ではない。Status を増やすと §7 の表・§7.1 / §7.2 の Work Item 表・
既存 Task の読み替えがすべて波及する。

`REVIEWING` の Status 行を含むコミット自身の SHA はそのファイルに書けない（書くと SHA が変わる）。
そのため **Implementation SHA は Task ファイルには書かず、Review Handoff（会話）と review report の Review Target に書く**。
Task の Status History の `TESTING → REVIEWING` 行には Branch だけを書く。

## Review Handoff

§5 に1か所だけ定義する（implementer.md / reviewer.md は参照のみ。`AGENTS.md` 冒頭「同じルールを他のファイルへ書き写さない」）。

最小項目:

| 項目 | 必須 | 内容 | 理由 |
| --- | --- | --- | --- |
| Target | 必須 | Task ID（Work Item を持つ Task は `<ID> / T-n`） | 承認単位の特定 |
| Branch | 必須 | push 先の作業ブランチ名 | fetch 先 |
| Implementation SHA | 必須 | push 済みの最終実装コミット（40桁） | レビュー対象の固定 |
| Diff range | 必須 | `<base-sha>..<implementation-sha>`（1コミットなら `<sha>^..<sha>`） | 複数コミット・同一ブランチに複数 Task が積まれる運用（U-4）で、差分範囲を一意にする |
| Task file | 必須 | `.ai/tasks/<ID>.md`（Work Item は `<ID>-<ITEM>.md` も） | Approval・Plan・AC・Files To Change・Implementation Result・Test Report・Analysis へのリンクがすべてここにある |

**渡さない（冗長）項目**:

| 項目 | 理由 |
| --- | --- |
| Approved Plan の場所 | Task file に含まれる |
| Analyzer report の場所 | Task file 冒頭の `Analysis:` 行（Task Template）にある |
| Implementation / Test Report の場所 | Task file 末尾の Implementation Result（implementer.md） |
| Changed Files | Diff range から機械的に得られる。Implementation Result の Changed Files との照合は Review Checklist #2 で行う |
| Test Scope | Test Report に含まれる（§14） |

例（MAGE-002 に当てはめた場合）:

```text
Review Handoff
- Target: MAGE-002
- Branch: claude/chapter-structure-scenario-test-q6oj4l
- Implementation SHA: 88fb6fc924bb5f7120401480a3874d45efd3c08b
- Diff range: 88fb6fc^..88fb6fc
- Task file: .ai/tasks/MAGE-002.md
```

## Implementer Responsibilities

implementer.md 手順4を次の2手順に置き換える（文案。確定文言は Implementation 時）:

4. テスト完了後、Human Approval 欄の Persistence が「許可」の場合だけ、承認範囲の成果物を commit し承認欄の Branch へ push する。
   Status を `REVIEWING` にする変更と Status History 行（Note に Branch）も同じ commit 群に含める。
   Persistence が「許可」でない・push できない場合は `TESTING` のまま止まり、人間に報告する（§7 P1）
5. push 済みであることを確認し（`git ls-remote` 等で remote の branch 先端に Implementation SHA が含まれる）、Review Handoff（§5）を出力する

制約:

- テスト完了前に push しない（T-1 F-13 を維持）
- push 先は承認欄の Branch だけ。`main` や他ブランチへ push しない。force push しない
- Files To Change 外のファイルを commit に含めない

Output Template の末尾に `### Review Handoff`（§5 の5項目）を追加する。ただし SHA は自己参照になるため、
Task file には Branch / Diff range の base までを書き、Implementation SHA は会話の Handoff にのみ書く
（Implementation 時にどちらか1形式に統一する。推奨: Task file には書かず会話のみ、正本は review report の Review Target）。

## Reviewer Responsibilities

### Target Verification（Review Order の 0番目として追加）

レビュー内容に入る前に、Handoff に対して次をすべて確認する:

| # | 確認 | 例 |
| --- | --- | --- |
| V-1 | Branch が remote に存在する | `git fetch origin <branch>` |
| V-2 | Implementation SHA が commit として存在する | `git cat-file -e <sha>^{commit}` |
| V-3 | SHA が Branch から到達可能 | `git merge-base --is-ancestor <sha> origin/<branch>` |
| V-4 | Diff range が空でなく、SHA が range の終点 | `git diff --stat <range>` |
| V-5 | SHA 時点の Task file に Human Approval（チェック済み）・Status `REVIEWING`・Implementation Result（Test Report を含む）がある | `git show <sha>:<task file>` |
| V-6 | SHA 時点に Task file の `Analysis:` が指す report が存在する | `git show <sha>:<analysis>` |

レビューは **Implementation SHA の内容** に対して行う。ブランチ先端・自分の working tree・未 commit 変更は対象にしない。

### 確認できない場合の扱い（既存用語のみ）

- `PASS` / `CHANGES_REQUIRED` を出さない（どちらも実装内容に対する判定であり、MAGE-002 F-01 / F-02 のような受け渡し不備を実装指摘として混ぜない）
- review report を作らず、会話で人間へ「**BLOCKED（理由: Review Handoff 不備）**」と、満たせなかった V-n を報告して止まる
- Reviewer は Task file の Status を変更しない（SHA が取得できない場合、書き込む対象の Task file 自体が確定しないため）。
  人間または Implementer が Handoff を再提示するか、必要なら Task を `BLOCKED` に記録する

### Output Template

reviewer.md の Output Template の `## Result` の前に追加:

```markdown
## Review Target
| 項目 | 値 |
| --- | --- |
| Target | <ID>（/ T-n） |
| Branch | |
| Reviewed SHA | |
| Diff range | |
| Target Verification | V-1〜V-6 OK |
```

MAGE-002 の Review Scope 表（EV-5）を正式化したもの。

## Reviewer Report Persistence

- Reviewer の Permission「READ ONLY」を次のとおり定義し直す: **コード・テスト・Task の計画本文・Approval 欄を変更しない**。
  書いてよいのは次の2つだけ:
  1. `.ai/reports/<ID>-review.md`（Work Item は `<ID>-<ITEM>-review.md`）
  2. 対象 Task file の `Status:` 行と Status History への1行追記（§7 で `REVIEWING` の担当は Reviewer）
- Reviewer はこの2つを **1コミット** にして、Handoff の Branch へ push する（Human Approval 欄の Persistence が許可している場合。D-2 / D-3）
- Review commit は Implementation SHA の子孫になる。`git diff <Reviewed SHA>..<review commit>` が上記2種類のファイル変更だけであることが、
  「レビュー後に実装が変わっていない」ことの機械的な証跡になる（MAGE-002 の `88fb6fc` → `4d7662f` と同じ形）
- PASS / CHANGES_REQUIRED の正本は push 済み review report の `Result` 欄。会話上の判定だけでは有効としない（D-3）
- push できない場合: Status は `REVIEWING` のまま、report 内容を会話で人間へ渡し、永続化されるまで DONE にしない

## DONE Gate

`REVIEWING → DONE`（§7 P2）の条件:

| # | 条件 | 採否 | 理由 |
| --- | --- | --- | --- |
| G-1 | 実装完了 | 含意 | TESTING / REVIEWING を経ている時点で成立。独立条件にしない |
| G-2 | 必要なテストが Test Report に記録されている | 含意 | 現行 §14 と Checklist #4〜6 で Reviewer が確認済み |
| G-3 | Reviewer の Result が PASS | **必須** | 現行どおり |
| G-4 | Reviewed SHA が最終実装 SHA と一致（Reviewed SHA 以降の commit が review report と Task の Status 行のみ） | **必須** | AC-6。CHANGES_REQUIRED 後の再実装は新しい Handoff と新しい Review Target を要する |
| G-5 | review report が remote の Branch に存在する | **必須** | AC-5 |
| G-6 | 未解決の BLOCKER / MAJOR が無い | 含意 | PASS の定義（Required Changes: None）に含まれる。独立条件にしない |
| G-7 | 完了についての Human Approval | **不要** | 現行 §7 は `REVIEWING → DONE` を Reviewer 担当としており、完了承認を要求していない。追加すると状態機械の変更になる。人間の統制は実装承認（§6）と push 許可（Persistence 行）で保つ |
| — | Full Regression | **不要** | §14 は Targeted を許容し、DC-3 は未決のまま。P-9 はこれを変えない（AC-8） |

DONE の記録は G-5 と同じ review commit で行う（Status 行と Status History 行）。

## Human Approval

（設計: Question 4 への回答）

**現行の解釈**: §6 の Human Approval は「その承認単位の Files To Change の範囲を実装してよい」ことだけを許可する。
commit / push について §6 は何も書いておらず、承認から push 許可を導く根拠は無い（P-9-analysis FACT 2）。
したがって **暗黙の push 許可は存在しない** と明記する。

**提案**: Approval 欄に Persistence 行を1行追加し、人間が明示的に選ぶ。

```markdown
## Human Approval
- [ ] Approved
- Approved by / date / where:
- Scope of approval:
- Persistence: 許可しない / 許可（branch: <作業ブランチ名>。テスト完了後の commit・push と、Reviewer の review report・Status の commit・push に限る）
```

§6 に追記する文言（文案）:

> Human Approval は、明記された範囲の実装だけを許可する。commit / push は Approval 欄の `Persistence` が `許可` で、
> ブランチ名が書かれている場合に限り、そのブランチへ、テスト完了後に行ってよい。`main` への push、force push、
> 承認範囲外のファイルの commit は、Persistence が許可でも許可されない。
> `Persistence` が空欄・`許可しない` の場合、Implementer は `TESTING` で止まり、push の GO を人間に求める（§7 P1）。
> AI は Persistence を自分で `許可` にしない。

効果:

- 「AI は自律的に push してよい」という読み方をさせない（許可は人間が branch 名つきで書いた場合だけ）
- MAGE-002 の「自動 commit・push はしない」は `Persistence: 許可しない` と同義として読める。その場合でも Protocol 上は
  「未 commit で REVIEWING に進む」ことが禁止されるため、MAGE-002 の失敗（EV-1）は起きない（TESTING で止まって GO を求める）

## Existing Task Migration

遡及修正はしない。変更後の規則は、**P-9 の Protocol 変更が入った後に `TESTING → REVIEWING` へ遷移する承認単位** から適用する。

| Task | 現状（@4d7662f） | 影響 |
| --- | --- | --- |
| CHAPTER-STRUCTURE / T-1 | DONE（同一セッション兼務） | なし。書き換えない |
| CHAPTER-STRUCTURE（Task Level） | PLANNED | なし。T-2〜T-4 は承認時に新しい Approval 欄（Persistence 行）を使う |
| MAGE-001 | WAITING_APPROVAL（実装を伴わない移行 Task） | なし。今後承認される場合は Persistence 行を記入する（push が不要なら `許可しない` でよい。実装差分が無ければ Handoff 対象も無い） |
| MAGE-002 | REVIEWING。PASS report が `4d7662f` に push 済み | 実質的に DONE Gate を満たしている: Reviewed SHA `88fb6fc` = 最終実装 SHA、`88fb6fc..4d7662f` は review report のみ（G-4 / G-5）、PASS（G-3）。未了は Status 行の DONE 記録だけ。**P-9 の実装には含めない**。記録するかどうか・いつ記録するかは人間の別指示で行う（Status History の Note に `Reviewed SHA 88fb6fc / review 4d7662f / P-9 適用前の承認のため Persistence 行なし` と書く案） |
| 旧形式の Approval 欄（Persistence 行が無い） | — | Persistence は「許可しない」として扱う（push は人間の GO 待ち）。既存の Approval 欄に行を追記しない |

## Acceptance Criteria

各 AC は、変更後の Protocol 文書を読んで判定する（テスト実行は不要）。加えて AC-9 で MAGE-002 の実績に机上適用する。

| AC | 内容 | 判定方法 |
| --- | --- | --- |
| AC-1 | Independent Reviewer が、レビューすべき実装コミットを一意に特定できる | §5 の Review Handoff に Branch・Implementation SHA（40桁）・Diff range が必須項目として定義されている |
| AC-2 | Reviewer が未 commit の working tree に依存しない | §7 P1 が push 済みを `TESTING → REVIEWING` の条件にし、reviewer.md が「Implementation SHA の内容をレビューし、working tree・ブランチ先端を対象にしない」と書いている |
| AC-3 | Handoff から承認済み実装と補助 report を取得できる | Handoff の Task file から Approval・Plan・Implementation Result・Test Report・`Analysis:` へ到達でき、V-5 / V-6 がそれを確認する |
| AC-4 | Reviewer がレビュー前に target SHA を検証する | reviewer.md の Review Order 先頭に V-1〜V-6 があり、失敗時は PASS / CHANGES_REQUIRED を出さず「BLOCKED（理由: Review Handoff 不備）」を報告すると書かれている |
| AC-5 | Task 完了前に review report が永続化される | §7 P2 に G-5（remote に review report が存在）がある |
| AC-6 | DONE には PASS と最終実装 SHA の一致が必要 | §7 P2 に G-4 があり、Review Target に Reviewed SHA 欄がある |
| AC-7 | 明示的 Human Approval を保ち、自律 push 権限を黙って与えない | §6 に「Approval は実装のみ許可」「push は Persistence: 許可＋branch 名がある場合だけ」「AI は自分で許可にしない」が書かれ、Task Template に Persistence 行がある |
| AC-8 | Targeted テスト方針が有効なまま | §14 を変更していない。DONE Gate に Full Regression が無い |
| AC-9 | MAGE-002 に机上適用して判定可能 | `88fb6fc`（Implementation SHA）/ `88fb6fc^..88fb6fc`（Diff range）/ `4d7662f`（review commit、差分は report のみ）で V-1〜V-6・G-3〜G-5 がすべて判定できることを Implementation Result に記録する |
| AC-10 | 新しい Status・Agent・テスト枠組みを追加していない | §7 の状態列・状態表の行数が変わっていない。`.ai/agents/` にファイルが増えていない |
| AC-11 | Review Handoff の定義が1か所だけ | Handoff の項目表は `AGENTS.md` §5 のみにあり、implementer.md / reviewer.md は参照している |

## Out of Scope

- ゲームコード（`src/`、`basefile.html`）・テスト（`tests/`）・Playwright 設定・`docs/`
- MAGE-002 の Status を DONE に記録すること（上記 Migration。人間の別指示）
- Reviewer の別セッション必須化（DC-1。P-9 は兼務・別セッションの両方で同じ手順が成立するようにするだけ）
- Full Regression の必須化（DC-3）
- Task ごとのブランチ運用（P-9-analysis U-4）
- Protocol の `main` への統合そのもの（D-4 で配置先だけ決める）
- stop hook 等、実行環境側の設定変更（T-1 Retrospective OPTIONAL）
- Analyzer / Planner の成果物の push 規則（P-9-analysis I-8 と同型の問題だが本題外。必要なら別 Task）

## Human Decisions

| ID | 決定事項 | 選択肢と影響 | Planner の推奨 |
| --- | --- | --- | --- |
| **D-1** | commit / push 方式 | **A**: 常に commit + push してから REVIEWING（例外なし）／ **D**: A を基本に同一セッション兼務のみ commit だけで可（兼務時は remote に対象が残らず、§5 の「人間による差分確認」も remote からは行えない）／ B・C は現行環境で MAGE-002 が再発するため非推奨 | **A** |
| **D-2** | Implementation の Human Approval が commit / push を含むか | **(a)** 含まない。Approval 欄の `Persistence` 行で人間が branch 名つきで明示した場合だけ許可（空欄＝不許可）／ **(b)** 含まない。push のたびに会話で個別 GO を求める（Persistence 行なし、handoff ごとに人間の操作が要る）／ **(c)** 承認に push を含める（「AI が自律的に push してよい」と読まれる余地が残る） | **(a)** |
| **D-3** | review report が永続化されるまで PASS を無効とするか。Reviewer に report と Status 行の commit / push を許すか | **(a)** 無効。Reviewer は report＋Status 行の1コミットを push（D-2 の Persistence 許可の範囲で）／ **(b)** 無効。ただし push は人間が行う（Reviewer は report を会話で渡す。人間の作業が増える）／ **(c)** 有効（会話上の PASS で DONE 可。監査証跡が残らない場合がある） | **(a)** |
| **D-4** | P-9 の Protocol 変更を載せるブランチ | **(a)** 現行 Protocol がある `claude/chapter-structure-scenario-test-q6oj4l` に直接／ **(b)** `4d7662f` から新しいブランチを切り、P-9 の analysis・本計画も同ブランチに集約してから変更（MAGE 系 Task の変更と分離できる）／ **(c)** 先に Protocol を `main` へ統合し、その後 `main` 起点で変更（統合は P-9 の範囲外で、別の判断が先に要る） | **(b)**（現行 Protocol を基準にしつつ、P-9 の成果物を1か所に揃えられる。P-9-analysis と本計画が別ブランチ・未 commit に分散していること自体が P-9 の問題と同型） |

D-1〜D-4 以外に人間の判断が必要な事項は無い（DONE の記録者は現行 §7 の Reviewer 担当に従い、完了承認は追加しない。変更したい場合のみ追加の判断となる）。

## Implementation Steps

承認後、D-4 で決めたブランチ上で行う。各 Step は Markdown の追記・置換のみ。

| Step | ファイル / 節 | 変更内容 | 理由 |
| --- | --- | --- | --- |
| 1 | `.ai/AGENTS.md` §4 末尾（L91-92） | 「残す」を「Implementer → Reviewer、Reviewer → DONE の受け渡しでは、作業ブランチへ commit・push して remote から取得できる状態にすること」と1〜2行で定義（§6 / §7 P1・P2 を参照） | FACT 5、I-1 |
| 2 | `.ai/AGENTS.md` §5 Reviewer の独立性（L121-123） | 入力を「Review Handoff の Implementation SHA 時点の Task / Plan / diff（Diff range）/ Test Report」に固定。Review Handoff の5項目表をここに置く。Reviewer の書き込み範囲（review report と Task の Status 行・Status History 行のみ）を1行 | FACT 3 / 7 / 9、AC-1 / AC-11 |
| 3 | `.ai/AGENTS.md` §5 Roles 表 Reviewer 行 | 変更権限を「**READ ONLY**（書くのは review report と Task の Status 行だけ）」に | FACT 9、I-6 |
| 4 | `.ai/AGENTS.md` §6 | 上記「Human Approval」の文案（実装のみ許可・Persistence 行・AI は自分で許可にしない） | U-1、AC-7 |
| 5 | `.ai/AGENTS.md` §7 状態表 TESTING / REVIEWING 行（L179 / L182） | 条件に P1 / P2 を追加。P2 の G-3〜G-5 は表の下に3行の箇条書き | FACT 8、AC-5 / AC-6 |
| 6 | `.ai/agents/implementer.md` Procedure 手順4、Output Template | 手順4 → 4・5 に分割（上記 Implementer Responsibilities）。Output Template に `### Review Handoff`（§5 参照） | FACT 6 |
| 7 | `.ai/agents/reviewer.md` 表の Permission / Input、Review Order、Output Template | Permission を §5 に合わせる。Input を「Review Handoff（§5）」に。Review Order の先頭に「0. Target Verification（V-1〜V-6。不可なら BLOCKED を報告して止まる）」。Output Template に `## Review Target`、末尾に「report と Status 行を1コミットで push して終える」 | FACT 3 / 12 / 13、AC-4 |
| 8 | `.ai/tasks/README.md` Task Template と Work Item の Approval 欄 | `- Persistence:` 行を1行ずつ追加（規則は §6 参照） | AC-7 |
| 9 | `.ai/tasks/P-9.md` 末尾 | Implementation Result。AC-1〜AC-11 を文書の該当行で判定（FACT (code) 相当＝文書確認）。AC-9 は MAGE-002 の実績（`88fb6fc` / `4d7662f`）への机上適用結果 | §8 |

Test Plan: ゲームコード・テストを変更しないため build / unit / E2E は実行しない（NOT_RUN、理由: Protocol 文書のみの変更）。
検証は AC の文書確認と AC-9 の机上適用で行う。

Rollback: Step 1〜8 のコミットを revert する（Markdown のみ。既存 Task・report は変更していないため影響は残らない）。

## Status History

| Date | Target | From → To | By | Note |
| --- | --- | --- | --- | --- |
| 2026-09-24 | Task | ANALYZING → WAITING_APPROVAL | Planner | 本計画を作成。Protocol は未変更。D-1〜D-4 は未決。作成時点では未コミット（作業ツリー: `claude/p9-artifact-handoff-planning-6pbacp`、`main` 起点）。**訂正（2026-09-24、人間の承認による）**: その後 `9e738d0` で `claude/protocol-artifact-handoff-p9`（`4d7662f` 起点）へ P-9-analysis とともに commit・push 済み |
| 2026-09-24 | Task | WAITING_APPROVAL → APPROVED | Human | D-1〜D-4 を承認（会話）。下の Human Approval |
| 2026-09-24 | Task | APPROVED → IMPLEMENTING | Implementer | `claude/protocol-artifact-handoff-p9` で Protocol 文書を変更 |
| 2026-09-24 | Task | IMPLEMENTING → TESTING | Implementer | 文書間の整合性確認・MAGE-002 実績への机上適用（下の Implementation Result）。ゲームのテストは対象外 |
| 2026-09-24 | Task | TESTING → REVIEWING | Implementer | Branch: `claude/protocol-artifact-handoff-p9`。Persistence 許可を受けて commit・push（AGENTS.md §7.3） |
| 2026-09-24 | Task | REVIEWING → CHANGES_REQUIRED | Reviewer | 別セッション。Reviewed SHA `25ff3fccd84ad3708e349f8e9346648744064dd2`。F-1（MAJOR）Persistence が Reviewer の commit を含むか未定義、F-2（MINOR）DONE 条件3。`.ai/reports/P-9-review.md` |

## Status

Status: CHANGES_REQUIRED
Human Approval: APPROVED（下記）
Persistence: 許可（branch: `claude/protocol-artifact-handoff-p9`）
Implementation: COMPLETE（末尾の Implementation Result）
Tests: build / unit / E2E は NOT_RUN（Protocol 文書のみの変更）。文書の整合性確認と机上適用を実施
Reviewer gate: CHANGES_REQUIRED（`../reports/P-9-review.md`）

## Human Approval

- [x] Approved
- Approved by / date / where: ユーザー（人間）/ 2026-09-24 / Claude Code セッションでの明示的な承認メッセージ（Implementer 作業の指示）
- Decisions: D-1 APPROVED（Option A: REVIEWING 前に commit・push）/ D-2 APPROVED（Human Approval と Persistence を分離。AI は Persistence を推測・補完しない）/ D-3 APPROVED（review report の remote への push 確認後に PASS が成立）/ D-4 APPROVED（`4d7662f` 起点の新ブランチ `claude/protocol-artifact-handoff-p9`）
- Scope of approval: 人間の Implementer 指示に基づく Protocol 文書（`.ai/AGENTS.md`、`.ai/agents/implementer.md` / `reviewer.md` / `planner.md`、`.ai/tasks/README.md`、`.ai/reports/README.md`）の変更と、本ファイルの Approval・Status・Status History・Implementation Result の更新。ゲームコード・`src/`・`tests/`・Playwright 設定・`basefile.html`・`docs/`・P-9-analysis は対象外。新しい Status・Agent・Task は作らない
- Persistence: 許可（branch: `claude/protocol-artifact-handoff-p9`）。ユーザー（人間）/ 2026-09-24 / Claude Code セッションでの明示的な指示「Commit and push to claude/protocol-artifact-handoff-p9 is allowed.」。対象: 上記6ファイルと本ファイル

## Implementation

**AUTHORIZED**（2026-09-24 承認。範囲は Human Approval の Scope of approval）

## Implementation Result

記入: Implementer（2026-09-24）。テンプレートは `../agents/implementer.md`。

### Changed Files

| ファイル | 変更 |
| --- | --- |
| `.ai/AGENTS.md` | §4: 図に「commit / push → Review Handoff」、「残す」を commit・push で remote から取得可能にすることと定義。§5: Roles 表（Implementer 出力に Handoff、Reviewer の入力と書き込み範囲）、Reviewer 禁止事項に Handoff 不備時の扱い、独立性の入力を Implementation SHA 時点に固定。**§5.1 Review Handoff**（5項目・Task file からの到達・V-1〜V-6・BLOCKED（理由: Review Handoff 不備））を新設。§6: Persistence。§7 状態表 TESTING / REVIEWING 行。**§7.3 Handoff and Completion Gates**（REVIEWING への手順、Reviewer の commit 範囲、DONE 条件、適用範囲）を新設 |
| `.ai/agents/implementer.md` | Output に Handoff。手順4を 4・5 に分割（Persistence 許可時のみ commit・push、到達確認と Handoff）。Review Handoff Template を追加 |
| `.ai/agents/reviewer.md` | Permission / Input / Task Status。Review Order に「0. Handoff 検証」、3 を Diff range の `git diff` に。Output Template に Review Target。末尾に1コミット push と PASS 成立条件 |
| `.ai/agents/planner.md` | 手順6: Persistence 行を空欄のまま残す |
| `.ai/tasks/README.md` | Task / Work Item の Approval 欄に Persistence 行。§6 / §7.3 への参照と、旧形式 Approval 欄の扱い |
| `.ai/reports/README.md` | Review report は push されて有効になる旨を1行（§7.3 参照） |
| `.ai/tasks/P-9.md` | 本節、Status、Human Approval、Status History（計画本文は変更していない） |

`.ai/reports/P-9-analysis.md`・`analyzer.md`・`debugger.md`・`decisions/README.md`・`.ai/` 外のファイルは変更していない。新しい Status・Agent・Task は追加していない。

**計画からの差分**（承認済みの人間の指示に基づくもの）:

- Scope 表は4ファイルだったが、`planner.md`（Persistence 行を空欄で残す）と `reports/README.md`（review report の有効条件）も変更した。人間の Implementer 指示が planner / reports/README を含む文書間の整合を求め、Persistence 許可が「six Protocol files」を対象としたため
- V-1〜V-6 は本計画の Reviewer Responsibilities ではなく、人間の Implementer 指示の定義（V-1 Branch 存在と SHA 到達 / V-2 Task file / V-3 Plan / V-4 Analyzer report / V-5 Implementation Result / V-6 Diff range 整合）に従った
- Reviewer の commit 範囲は PASS に限らず CHANGES_REQUIRED にも同じく適用した（受け渡しの証跡を揃えるため）
- DONE 条件の「最新 Implementation SHA と一致」は、同じブランチに複数 Task が積まれる運用を考慮し「Reviewed SHA より後に、その承認単位の Files To Change へのコミットが無い」と定義した

### Test Report
- Scope: Targeted（Protocol 文書のみの変更）
- Executed:
  - 状態名の検索（`.ai/AGENTS.md`・`.ai/agents/*.md`・`tasks/README.md`・`reports/README.md`）: 使われている状態名は §7 の13種だけ
  - `§` 参照の検索: すべて実在する節（新設 §5.1 / §7.3 を含む）を指す
  - Handoff 項目・V-1〜V-6 の定義が AGENTS.md §5.1 の1か所だけにあり、agents 側は参照・書式のみであることを確認
  - 変更差分の通読（Work Item の Status 正本 §7.2 と §7.3 の Reviewer commit 範囲の整合、Debugger / Analyzer の責務に変更が無いこと、§12 / §14 未変更）
  - MAGE-002 の実績への机上適用（`git` コマンドで確認。下記 AC-9）
- Why this scope: ゲームコード・テストを変更していない
- Not run: `npm run build` / `npm run test:unit` / `npm test`（ゲーム側の変更が無いため）
- Environment: 回避策なし

| テスト | 結果（PASS / FAIL / FLAKY / NOT_RUN） | メモ |
| --- | --- | --- |
| 状態名の整合 | PASS | 新しい状態名なし |
| 節参照の整合 | PASS | |
| Handoff 定義の単一性 | PASS | |
| MAGE-002 への机上適用 | PASS | AC-9 |
| `npm run build` / `test:unit` / `npm test` | NOT_RUN | Protocol 文書のみ |

作業中、`AGENTS.md` §4 の既存文「無いまま」の1文字を誤って削除し、commit 前に元へ戻した（最終差分に含まれない）。

### Acceptance Criteria

| AC | 確認方法（VERIFIED / FACT (code)） | 根拠 |
| --- | --- | --- |
| AC-1 | FACT (code) | AGENTS.md §5.1 の Handoff 表（Branch / Implementation SHA 40桁 / Diff range） |
| AC-2 | FACT (code) | §4「working tree にだけある成果物は受け渡し済みとみなさない」、§7.3 手順2〜4、§5.1 末尾「未 commit の変更は対象にしない」 |
| AC-3 | FACT (code) | §5.1「Task file から次のとおり辿れる」と V-2〜V-5 |
| AC-4 | FACT (code) | reviewer.md Review Order 0、§5.1 V-1〜V-6 と BLOCKED（理由: Review Handoff 不備） |
| AC-5 | FACT (code) | §7.3 DONE 条件2 |
| AC-6 | FACT (code) | §7.3 DONE 条件3、reviewer.md Review Target の Reviewed SHA |
| AC-7 | FACT (code) | §6 Persistence、tasks/README の Persistence 行、planner.md 手順6 |
| AC-8 | FACT (code) | §14 未変更、§7.3「Full Regression と追加の Human Approval は DONE の条件にしない」 |
| AC-9 | VERIFIED（机上適用） | `88fb6fc`: V-1 commit かつ `origin/claude/chapter-structure-scenario-test-q6oj4l` から到達可能 / V-2 `.ai/tasks/MAGE-002.md` あり / V-3・V-5 計画と Implementation Result（Changed Files・Test Report）あり / V-4 `MAGE-002-analysis.md` あり / V-6 `88fb6fc^..88fb6fc` は3ファイル +615 で終点一致。DONE 条件: PASS（`MAGE-002-review.md`）、report は remote（`4d7662f`）、`88fb6fc..` 以降のコミットは `4d7662f`（review report のみ）だけ。いずれも判定可能 |
| AC-10 | FACT (code) | §7 状態列・状態表の行数不変。`.ai/agents/` のファイル数不変 |
| AC-11 | FACT (code) | Handoff 項目表は §5.1 のみ。implementer.md は書式、reviewer.md は参照 |

### Out of Scope Found

- MAGE-002 の `Status: DONE` 記録（Existing Task Migration のとおり人間の別指示）
- Analyzer / Planner 成果物の push 規則（Out of Scope のとおり）
