# 見下ろし型ARPG 統合方針 ―― 解析・設計・優先順位

「自キャラモーション」「敵・ボスデザイン」「カメラ・光エフェクト」
「探索・戦闘ゲームデザイン」を一つのループへ統合するための解析結果と設計。
実装記録は `COMBAT_DESIGN.md`(戦闘・職業)と併読すること ―― あちらは
戦闘システムの経緯、こちらは探索も含めた全体方針を扱う。

目標ループ:

```
広いフィールドを探索する
 → 遮蔽物や視界制限の向こうを覗く
 → 敵を発見する
 → 細かく高速な連撃で戦う
 → 瀕死になったら処刑でフィニッシュ
 → ネームドを探す
 → 撃破してスキル・レシピを得る
 → 強化してさらに探索する
```

吸血鬼ものから持ち込むのは**ゲーム構造だけ**(環境による探索制約・視界・
遭遇・戦闘テンポ・処刑・ネームド探索・リスク&リターン)。吸血鬼設定、
吸血、太陽が弱点といった設定は一切導入しない。本作の世界観(中世的・
怪異・自然現象・人々の生活・少し幻想的・過度に荒廃していない)は維持する。

---

## PHASE 1. リポジトリ解析結果

### 1-1. 構成

`basefile.html`(凍結スナップショット)→ Vite 構成へ移行済み。詳細は
`ARCHITECTURE.md`。要点だけ再掲すると:

| 層 | 内容 | 触り方 |
|---|---|---|
| `src/core/*.js` | state に依存しない**純粋計算**。`tests/unit/` から直接テストできる | 新規ロジックはまずここへ |
| `src/legacy/parts/01〜14-*.js` | 約20,000行。`import`/`export` を持たず、Viteプラグインが**1つの共有スコープへ連結**する | 既存の関数へ**最小の呼び出しを足す**だけ |
| `src/legacy/concat-plugin.js` | 連結プラグイン。`core/` からの import はここの HEADER に書く | 新しい core モジュールを使うときだけ |
| `basefile.html` | 凍結。**更新しない** | 触らない |

**約90個の共有可変変数**(`scene` / `enemies` / `walls` / `player` /
`playerMixerParts` …)を素の `let` で持ち、あちこちから再代入している。
ESモジュール化の一括変換はリスクが高すぎるという判断が既に下されている
ので、**今回もその判断を踏襲する**。

### 1-2. モーション基盤(実測)

- ポーズは `CLIPS`(`05-rendering-rig.js`)というキーフレーム表。
  チャンネルは `waist / shL / elL / shR / elR / hipL / hipR / kneeL / kneeR`、
  武器は `wep`(刃の向き+刃先の向きの6要素)、体の変位は `push / drop / lift`。
- 再生は `beginMove(clip)` → `state.swinging` / `state.swingT` →
  `applyCombatPose()` が `sampleClip()` の結果を `applyPose()` へ渡す。
- 構えは `STANCE[クラス]` / `STANCE_ALT[武器種]`、`activeStance()` が選ぶ。
- 歩行・呼吸・着地・回避・低HP・被弾の仰け反りは `updateLocomotion()`
  (`13-update-loop.js`)が担当。`applyCombatPose()` はその**後**に走るので、
  攻撃中は攻撃が歩行に勝つ。
- 上位職差は `JOB_ATTACK_TEMPO`(再生速度)、`JOB_SWING_AMPLIFY`(可動域)、
  `core/swing-timing.js`(静→動の配分・Hit判定・SEの同期)。

### 1-3. 指示にあった前提と実際の差異(重要)

指示文に出てくる次のものは、**このリポジトリには存在しない**。
別プロジェクト、あるいは未pushの作業と取り違えている可能性が高い。

| 指示にあった前提 | 実際 |
|---|---|
| `src/core/character-motion-state.js` | 無い |
| `src/core/pose-geometry.js` | 無い |
| `src/core/combat-stances.js` | 無い(構えは `05-rendering-rig.js` の `STANCE` 表) |
| `src/core/motion-poses.js` | 無い(ポーズは `CLIPS` 表) |
| `src/core/head-rig.js` | 無い |
| SOCIAL → EXPLORATION → DRAWING → COMBAT → POST_COMBAT → SHEATHING の状態機械 | 無い。該当する識別子はリポジトリ全体で0件 |
| Eye Rig(EyePivot / localEyeYaw ±10° / follow 16/s / linger 0.45s) | 無い。`EyePivot` `localEyeYaw` ともに0件。目は `makeEyeSclera/Pupil/Highlight` の静的メッシュで、視線追従の仕組みそのものが無い |
| Visual Waist Look(dead zone 24° / cap ±12.6° × 職業係数) | 無い |
| Unit 635/635 | 実測 **536/536 pass**(`npm run test:unit`、`node_modules` 導入後) |
| Motion E2E 8/8 | 該当スペックが無い |
| Full E2E 45 passed / 1 flaky / 3 failed | スペック総数は **41 tests / 15 files** |

**したがって「既存のEye Rig仕様を壊さないこと」という制約は、壊す対象が
存在しないため自動的に満たされる。** 一方で「視線 → 頭 → 体幹 → 構え の
順に分散させる」という要求は、Eye Rig を**新規に作る**話になる(後述の
優先度A-7へ回した ―― 目のメッシュは頭に直付けで、ピボットの追加は
`makeCharacterHead()` の構造変更を伴うため、視界・戦闘テンポより
先にやる価値が薄いと判断した)。

### 1-4. カメラ

- `state.camYaw / camDist(6) / camHeight(8)`(見下ろし約53°)。
- 右スティック(`gp.axes[2]`)・Q/E・タッチボタン → `camYaw` を手動回転。
- 自動追従: 移動方向の後ろへ `camYaw` を寄せる。手動入力から 1.4 秒は割り込まない。
  真横への振り向きだけ速く、前後は遅くする `sin^3` の緩急付き(酔い対策として既に調整済み)。
- 戦闘時: `getCombatCameraFocusOffset()` が近くの敵を重み付き重心にして
  注視点をずらす。予兆中(`punishWindowState`)の敵は重みが1.8倍。
- ボス: `findLockOnBoss()` 中は手動回転を**完全に無視**するロックオン。

### 1-5. 敵

- スポーンは座標直書きのテーブル(`07-ai-combat.js` の `SPAWNS`)。
  `wanderTarget` による徘徊、`respawnT=20` での再出現あり。
- タイプ: `atkType: charge / fire / kite / ghost`、`guardian`(常時ガード)、
  `turret`(固定)、`flying`、`strongMob`(1.5倍)、`midbossName`(1.7倍・名乗り付き)。
- 体幹(posture)・ダウン・パニッシュ窓・予兆は実装済み(`COMBAT_DESIGN.md` 8章)。
- ダメージは `dealDamageToEnemy()` **1本に集約**されている。撃破は `finishEnemyDeath()`。

### 1-6. 探索・視界

- `hasLineOfSight(a,b)`(`02-world-common.js`)が既にある。`walls` の
  AABB に対する線分サンプリングで、**敵AIが壁越しにプレイヤーを見つけない**
  ために使われている。
- 一方で描画側は逆を向いていた:
  - `addXrayShell()` が**全ての敵に「壁の向こうでも光る輪郭」を付けている**
    (`depthFunc: GreaterDepth`)。壁の向こうの敵が常に見える。
  - ミニマップが**生きている敵を全て正確な位置で**描いている。
  - `scene.fog` はワールドごとの雰囲気付けで、情報制御には使っていない。

### 1-7. スキル・武器・必殺技

- スキル枠は**既に実質2枠**: スキル1(`state.skillChoice`、`retreat`/`spin`/
  `barrier`/解放技/上位職専用から選択)+ スキル2(`state.skill2Choice`、
  クラス既定/alt)。さらに必殺技(`ult`)とボス能力枠がある。
- 武器切り替えも**既にある**: `WEAPON_TYPES`(クラスごとに native/alt)、
  `state.usingAltWeapon`、専用の構え(`STANCE_ALT`)と専用クリップ
  (`altBasic`/`altBasic2`)。切り替えは装備UI経由。
- 必殺技は `JOB_ULT_BY_JOB` / `CLASSES[].ult` でクラス・上位職ごとに存在。

---

## PHASE 2. 今回の要求との乖離

| # | 要求 | 現状 | 判定 |
|---|---|---|---|
| S-1 | 遮蔽物による視界制限 | **逆向き**。xrayシェルとミニマップが壁越しに敵を全部見せている | **変更** |
| S-2 | 高速連撃に対応したモーション基盤 | クリップの途中割り込みは既に成立している(次の `beginMove()` が上書きする)。基盤は足りている | **維持** |
| S-3 | 攻撃後の自然な return | **姿勢の飛びは無かった**(下記 2-2 の訂正)。実際の問題は、振り終わりから次の入力までの 0.16 秒(剣士)が**完全な静止画**になること | **変更** |
| S-4 | 処刑システム | 無い。ただし `dealDamageToEnemy()` という単一の合流点があるので足しやすい | **新規** |
| S-5 | カメラ操作・探索視点 | 右スティック=カメラ回転は既にある。要求の「通常時は視点/攻撃方向、カメラボタン中だけ回転」は**現状と別設計** | **保留**(下記参照) |
| S-6 | 敵の視認/戦闘時ハイライト | 予兆の色変化はあるが、可視段階と連動していない | **変更** |
| A-1 | スキル2枠構造 | **既に2枠相当**。作り直す必要が無い | **維持** |
| A-2 | ネームド方向マーキング | `midbossName` 個体はあるが方向表示が無い | **新規** |
| A-3 | 武器切り替え | **実装済み**(装備UI経由)。十字キー割り当ては別問題 | **維持** |
| A-4 | 武器別必殺技 | クラス/上位職別にはある。**武器種別**には無い | **新規** |
| A-5 | 共通リキャスト | `ult` は既に共通クールダウン | **維持** |
| A-6 | 敵デザイン刷新 | 型は揃っている。配置密度とHP設計が課題 | **変更** |
| A-7 | 視線(Eye Rig) | 存在しなかった。**今回新規実装**(下記 4-4) | **新規** |
| B-* | 強モブ/マップ変異/高難易度/高度な光演出 | `strongMob`・周回制限時間・星評価はある | **保留** |

### 2-2. 解析の途中で訂正した見立て(重要)

最初は「クリップ終了の瞬間に、構え(`STANCE`)から休め姿勢(`armLBase`)へ
1フレームで飛んでいる」と考えた。コードを追った結果、**これは誤りだった**:

- `buildPlayer()`(`06-player-enemy.js`、「class stance, straight out of the
  choreography table」のブロック)が、腕の基準姿勢 `armLBase`/`armRBase`/
  `elbowLBase` を **`activeStance()` の構えそのもの** から取っている。
- クリップの終端フレームも同じ構え(`F(1.00, S(cls))`)。
- `updateLocomotion()` が書くのは `armLBase` を中心とした腕振りなので、
  振り終わりの接続は**元々連続している**。

**実際の問題は姿勢ではなく時間だった。** 剣士はクリップ 0.36 秒に対し
攻撃CDが 0.52 秒。その差の 0.16 秒、`applyCombatPose()` は何のポーズも
当てておらず、立ち止まって連打していれば歩行サイクルも動かないので、
文字どおり静止画になる。「一撃ごとに止まって見える」の実体はこれ。

したがって対処も変わった:
- **やったこと**: その静止時間を「息をしている絵」に変える(Combat Idle)。
  加えて振り終わり直後だけ揺れを大きくし(`settleBoost`)、
  attack → recovery → next attack の流れを作る。
- **やっていないこと**: 攻撃間隔そのものを詰めること。クリップ長と
  `atkCooldown` を動かすのは DPS を含む戦闘バランスの変更で、
  影響範囲が別物になる(PHASE 5-2 の順序を参照)。

### 2-1. 衝突していて「勝手に変えない」と判断した点

1. **攻撃ボタン長押し = 自動連撃(要求)vs 長押し = 溜め技(現状)**
   `ATTACK_TAP_THRESHOLD = 0.5` 秒で、長押しは既に「溜め技(dash)」へ
   割り当て済み。ここを自動連撃に変えると溜め技が出せなくなる。
   → **保留。** モーション側が連撃を受け止められるかだけ確認した(受け止められる)。
   入力設計を変えるなら別途合意が要る。

2. **十字キー = 武器/アイテム切り替え(要求)vs 現状の割り当て**
   D-pad上=シナリオ選択、下=鑑定、左=ボススキル3、右=アリーナ巡回スポーン。
   4方向すべて埋まっている。
   → **保留。** どれを移すかの判断が要る。

3. **右スティック = 視点/攻撃方向、カメラボタン中だけ回転(要求)**
   現状は右スティック=常にカメラ回転。攻撃方向は移動入力+ソフトロック
   (`core/soft-lock.js`)・ターンアシスト(`core/predictive-aim.js`)で
   決まっており、職業アイデンティティの一部になっている。
   → **保留。** 変えると鷹の目/バーサーカーの設計が崩れる。

4. **敵HP「連撃10回以上」**
   実測(`tests/base-class-comparison.spec.js` のログ)で単発
   119/102/92/129 に対し、洋館帯の雑魚HPは58〜190。**現状1〜2発で死ぬ。**
   要求の「10発以上」にするには4〜8倍のHPが要る。
   → **保留(要判断)。** 単純なHP増加は禁止とも書かれている。
   攻撃間隔・ヒット感・リアクションを先に作ってから、数値は最後に合わせる
   のが正しい順序なので、今回は**触らない**。詳細は PHASE 5-2。

---

## PHASE 3. 依存関係

```
 探索視界(S-1) ──┬─ hasLineOfSight()          [既存・共有]
                  ├─ addXrayShell()            [描画: 壁越しの輪郭]
                  ├─ drawMinimap()             [HUD: 敵の点]
                  └─ threatHighlight(S-6)      [ハイライト強度]

 モーション(S-3) ─┬─ applyCombatPose()        [唯一のポーズ適用点]
                  ├─ updateLocomotion()        [その直前に走る歩行]
                  └─ beginMove()               [全クリップ再生の合流点]

 処刑(S-4) ───────┬─ dealDamageToEnemy()       [全ダメージの合流点]
                  ├─ finishEnemyDeath()        [全撃破の合流点]
                  └─ 体幹/ダウン(既存)        [「決めに行く」条件に使う]

 ネームド(A-2) ───┬─ midbossName(既存)
                  └─ drawMinimap() / HUD
```

**合流点が1箇所ずつ存在する**のがこのリポジトリの強みで、今回の統合は
すべて「合流点へ数行足す」形に落とせる。大規模な書き換えは要らない。

---

## PHASE 4. 設計 ―― 既存を壊さない最小単位

新しいロジックはすべて `src/core/` の純粋関数として切り出し、
`parts/` 側は呼び出すだけにする(`core/damage-math.js` 等と同じ方式)。
ゲームを起動せずに `tests/unit/` で検証できることが、この巨大な
共有スコープに手を入れる際の唯一の安全網になる。

### 4-1. `src/core/combat-stance.js` ―― Combat Idle と自然な return

- `combatStanceWeight(remainT, fade)` 戦闘態勢の残り時間 → 適用ウェイト。
  最後の 0.75 秒を smoothstep でフェードさせる = ぶつ切りにしない。
- `refreshCombatStance(t, hold)` 攻撃・被弾・敵接近で 2.6 秒へ伸ばす。
- `idleProfile(classKey, jobKey)` 職業ごとの重心・呼吸・武器の揺れ・周期。
  剣士=ゆっくり大きい / 盗賊=小刻みで速い / 魔法使い=ほぼ静止 /
  弓師=上体静止。上位職は倍率で上乗せ(`JOB_IDLE_MUL`)。
- `combatIdleOffsets(phase, profile, w, amp)` 構えへの**加算値**だけを返す。
  位相には既存の `strideT` を渡す(新しいタイマーを増やさない)。
  `amp` は `w` と別軸(クランプしない)―― 一緒にすると振り終わり直後の
  上乗せが `w=1` で頭打ちになって効かなくなる。
- `settleBoost(sinceSwingEnd)` 振り終わった直後だけ揺れの振幅を 2.4 倍から
  0.30 秒かけて 1 倍へ落とす。毎回きっちり同じ構えへ戻り切ってから次を
  振る、という止まった印象を減らす = **attack → recovery → next attack**。
- `blendPose(a, b, w)` ポーズ補間。
- `swingGapSeconds(swingDur, atkCooldown)` 「振り終わって待っている」時間。
  調整とテストのための可視化。

**適用点は `applyCombatPose()` の末尾に分岐を1つ足すだけ。**
`CLIPS` にも `STANCE` にも触らないので、攻撃・スキル・回避・必殺技の型は
一切変わらない。補間の起点は「今フレームの歩行が書いた姿勢そのもの」に
するので、ウェイトが低い間は歩行の腕振りが残り、歩きながら構える動きが
自然に出る。

### 4-2. `src/core/enemy-visibility.js` ―― 情報を限定する

3段階。**真っ暗にはしない**。

| 段階 | 条件 | 表示 |
|---|---|---|
| `visible` | 遮蔽が無く索敵距離(34)の内側 | 今まで通り |
| `sensed` | 直前まで見えていた(2.2秒の記憶)/ 交戦中で14m以内 | 輪郭だけが薄く残る。ミニマップには出さない |
| `hidden` | それ以外 | 情報を出さない |

- `sensed` を挟むのが肝。これが無いと「壁の陰に入った瞬間に敵が消える」。
- 交戦中(`triggered`)で近い敵は**必ず `sensed` 以上**。追ってきている敵を
  見失って一方的に殴られる理不尽を作らない。
- **ボスは常に `visible`。** ダイアログ・フェーズ・ロックオンカメラが
  すべて「見えている」前提なので、既存のボス戦を一切変えない。
- `minimapVisible(level)` 見えている敵だけを点で出す。
- `threatHighlight({level, triggered, windup, finishable})` 通常時0、
  交戦0.18、瀕死0.60、予兆0.75。**常に画面が光っている状態にはしない。**
- `bearingLabel(fromX, fromZ, toX, toZ)` 8方位だけを返す(ネームド用)。
  距離は返さない = 位置を漏らさない。

遮蔽判定は既存の `hasLineOfSight()` をそのまま使う。新しい遮蔽システムは
作らない ―― 敵AIが既に使っている実績のある判定を、描画側にも共有させるだけ。

### 4-3. `src/core/execution.js` ―― 処刑

- `isFinishable(en, 0.10)` HPが10%以下。**ボスは対象外**(専用の撃破演出を持つため)。
- `canExecute(en, {isFinish, knockedDown})` 瀕死**かつ**プレイヤーが
  「決めに行った」証拠(コンボのフィニッシュ段、またはダウン中への追撃)。
  瀕死の敵を連打で勝手に処刑してしまうと「締めた」感触にならないため、
  自動発動にはしない。
- `executionStyle(classKey, jobKey)` 世界観に合わせた所作:
  剣士=断ち斬り / 盗賊=影断ち / 魔法使い=祓い / 弓師=射抜き /
  戦騎士=兜断ち / バーサーカー=叩き伏せ / 魔導士=封印 / 鷹の目=一矢。
  **吸血・血の所作は入れない**(ユニットテストで機械的に検査している)。
- `executionDamage(en, base)` 必ず削り切る。「処刑したのに生き残る」が
  一番興ざめなので。

適用点は `dealDamageToEnemy()` 1箇所。

### 4-4. `src/core/look-rig.js` ―― Eye Rig / Visual Look Offset

引き継ぎ資料の設計値をそのまま実装した。存在しなかったので新規。

| 部位 | 可動域 | 備考 |
|---|---|---|
| 目 | yaw ±10° / pitch ±7° | 追従 16/s、見失っても 0.45 秒は保持 |
| 頭 | yaw ±38° / pitch ±20° | 上位職は `JOB_HEAD_LOOK_MUL` で制限(下記) |
| 体幹 | デッドゾーン 24°、超過の 45% を ±12.6°×係数 | pitch は ±5°×係数 |

職業係数(どれだけ体幹で見るか): 盗賊 0.60 / 剣士 0.70 / 弓師 0.80 /
魔法使い 1.00。魔法使いだけ pitch 係数 0 ―― 杖を構えた上体を上下に振ると
詠唱の姿勢が崩れて見えるため。

**chain の配分をひとつだけ変えた。** 資料は
`target → body差分 → waist → final waist差分 → head → 残りを eyes` と
書かれているが、字義どおり「頭が可動域いっぱい(±38°)まで先に吸い、
余りを目へ」と実装すると、**頭の可動域の内側では目がまったく動かない**
(15° を見ると頭だけが 15° 回り、目は中心のまま)。Eye Rig を入れる意味が
無くなるので、**目に ±10° のリードを与え、その先を頭が引き受ける**形に
した。角度が大きい領域では目は上限に張り付き残りはすべて頭 ―― つまり
資料の chain と同じ結果になる。違いが出るのは小さく見る領域だけで、
そこは目が動くのが正しい。

```
  3° → waist 0.0  head  0.0  eye  3.0
 15° → waist 0.0  head  5.0  eye 10.0
 40° → waist 7.2  head 22.8  eye 10.0
 60° → waist 8.8  head 38.0  eye 10.0
```

**リグの構造**(`buildPlayer()` の LOOK RIG ブロック)

```
waist
 └ headLookPivot   頭+髪+その職の被り物ごと回る(原点=頭の中心)
     ├ head / hair / bangs / sideHair / backHair / headwear
     └ eyePivot    目だけの回転(頭の中でさらに動く)
         └ sclera / pupil / highlight ×2
```

目は今まで `head` と兄弟のメッシュとして直に置かれていて、「どこを見て
いるか」を表現する手段が一切無かった。被り物は「頭のもの(兜・フード・
帽子・つば)」と「首から下のもの(襟・マント・矢筒)」が同じブロックに
並んでいるので、Y座標などで機械的に振り分けず**作った場所で明示的に
push** している。

`headLookPivot` の原点は、戦騎士の昇格処理が頭部縮小のために作っていた
`headPivot` とまったく同じ位置。あちらはこのピボットへ `scale` を掛ける
だけになり、付け替えの往復(`headScaleGroup`)ごと消えた。

**ターゲット方針**(資料の状態機械を、この実装に実際にある状態へ対応付け)

| 資料の状態 | この実装での判定 | 見る相手 |
|---|---|---|
| combat / drawing / post-combat | `state.combatStanceT > 0` | 最も近い**見えている**生存敵 |
| sheathing | 戦闘態勢が切れていく間 | linger(0.45秒)が直前の方向を保持 |
| exploration / social | それ以外 | 近くの仲間 → いなければゆっくり見回す |

視界制限で `hidden` の敵は**選ばない**。そちらへ顔を向けると、壁の向こうの
敵の位置を視線が漏らしてしまい、S-1 で作った緊張感が台無しになる。

**上位職の被り物**: `applyJobPromotionVisual()` が作る被り物・髪・髭も
頭ピボットへ載せてある(`addHeadPart()`)。バーサーカーの髪スパイク/
こめかみの房/長髪/髭、魔導士の帽子の房飾り/長い毛束、鷹の目の眼帯/
ベルト/フードが対象で、胸鎧・腰鎧・マント・矢筒といった首から下のものは
体幹に残す。

**唯一の例外が戦騎士**で、`JOB_HEAD_LOOK_MUL.battleKnight = 0`。あの職は
頭身を詰めるために頭ピボット自体へ 0.86 の scale を掛けており、同じ
ピボットへ兜を載せると兜まで二重に縮む。全面を覆う兜で頭の回転が
そもそも見えない職なので、体幹と目だけで見る。

### 4-6. メイン武器の必殺技モーション(`src/core/ult-clips.js`)

#### 解析で分かった2つの構造的な問題

1. **上位職4種に専用の型が無かった。** 8職に対し ult クリップは4本で、
   上位職は `JOB_ATTACK_TEMPO`(再生速度)だけを変えて基礎職と同じ振りを
   していた。長剣・両手小斧・大弓・より大きな魔法という「一回り上の
   得物」になっているのに、振りの形が同じ。
2. **必殺技だけタイムラインが揃っていなかった。** 通常攻撃は
   `core/swing-timing.js` が「判定・SE・見た目」を1点へ集約しているが、
   必殺技は対象外で、`fireUltimate()` が入力の瞬間にダメージも VFX も SE も
   全部出し、モーションだけが後から振られていた。剣士の渾身の斬撃は、
   剣が当たりに行くより 0.3 秒早く敵が吹き飛んでいた。

#### やったこと

**8職ぶんの型を書き起こし、core へ出した。** キーフレームを core に
置いたのは、「力の伝達」をユニットテストで縛るため ―― 言葉のコメントでは
あとから1フレーム足した瞬間に「腕だけ動く振り」へ戻ってしまう。

`tests/unit/ult-clips.test.js` が全クリップに対して機械的に検査する:

- 2枚目(沈み)の時点で**脚が既に動いている**(腕だけの振りにならない)
- **腕が伸び切るのは脚が動き出した後**
- **武器が振り抜けるのは腕が動き出した後**(武器だけ振らない)
- 戻りでは**腰が腕より先に構えへ帰っている**(反動が上から下へ抜ける)
- 終端は必ずその職の構えそのもの(次の行動へ素直に繋がる)
- 可動域が常識の範囲(人形が壊れない)

1本のクリップは必ず次の役割で構成する:

```
0.00 構え → 0.12 沈み → 0.30 駆動 → 0.45 接触 → 0.62 吸収 → 0.82 巻戻し → 1.00 構え
       脚が載る   腰が回る   肩・腕・武器   膝が受ける   腰が先に戻る
```

| 職 / 武器 | 型 |
|---|---|
| 剣士 大剣 | 深く沈んで担ぎ上げ、体重ごと振り下ろす。踏み込み最大 `push 0.58` |
| 戦騎士 長剣 | 受けの構えから最短距離で突き込む。踏み込みは剣士の約半分 |
| 盗賊 双剣 | 地面すれすれまで沈み、捻りを一気に解く。戻りを短く切り上げる |
| バーサーカー 両手小斧 | まず脚だけ沈み、遅れて斧が上がる。`drop 0.56` で最も深く、反動も最大 |
| 魔法使い 杖 | 杖を胸元へ引き寄せてから天へ。発動後に余韻 |
| 魔導士 | 両腕を大きく開いて力を集め、前へ叩きつけ、**放った力に身体が押し返される** |
| 弓師 小弓 | 短い溜めから3連射。射撃後に弓手が開いて肩が落ちる |
| 鷹の目 大弓 | 背中から引く長い溜め(`draw` が t=0.40 でようやく 1)。放った後に上体が流れる |

**当たる瞬間とモーションを揃えた。** 入力レスポンス(ゲージ消費・
ロックアウト・モーション開始)は従来どおり即座に走らせ、ダメージ・VFX・
SE・画面の閃光だけを「接触フレーム」まで保留する(`state.pendingUlt`、
通常攻撃の `pendingSwing` と同じ仕組み)。

ただし素直に掛け算すると魔導士・鷹の目で 0.45〜0.47 秒になり、
「必殺技だけ極端に硬直する」状態になる。今の戦闘は短い攻撃間隔と細かい
連撃を目指しているのでそちらを優先し、**上限 0.30 秒**で頭を打たせた。
上限に当たった技はクリップの進み方を折り曲げて(`ultClipWarp`)、
溜めの形を保ったまま接触フレームだけをその瞬間へ重ねる ―― 戦騎士の
通常攻撃で既に使っている時間伸縮と同じ考え方。

| 職 | クリップ長 | 当たる瞬間 |
|---|---|---|
| 剣士 | 0.72s | 0.300s |
| 戦騎士 | 0.78s | 0.300s |
| 盗賊 | 0.50s | 0.170s |
| バーサーカー | 0.61s | 0.255s |
| 魔法使い | 0.68s | 0.300s |
| 魔導士 | 0.88s | 0.300s |
| 弓師 | 0.85s | 0.170s |
| 鷹の目 | 1.06s | 0.300s |

#### 演出の階層 ―― 通常ヒット < 必殺技 < 処刑

調べたところ**処刑が必殺技より弱く、順序が逆転していた**。処刑の
`shake` は 0.048〜0.090 で、通常ヒットの 0.06 と同程度。原因は、共有の
`hitStop()` が上限 0.022 秒 + 不応期つきで、直前の通常ヒットが不応期を
消費した後は処刑の分が必ず無視されていたこと。

`hitStop()` に「不応期を無視する / 上限を引き上げる」任意の指定を足した。
**省略時の挙動は従来とまったく同じ**なので通常ヒットの手触りは変わらない。

| | shake | hitStop | 閃光 |
|---|---|---|---|
| 通常ヒット | 0.06 | 0.016(上限 0.022) | なし |
| 必殺技 | 0.16 | 0.05(上限 0.055) | あり |
| 処刑 | 0.19〜0.30 | 0.060〜0.085(上限 0.09) | あり |

順序が逆転していないかは `tests/unit/execution.test.js` が検査する。

#### Eye Rig との干渉

旋回する必殺技(八方の矢・阿修羅・天翔ける鏃)の最中は Look Rig を
止めるようにした。あの間 `player.rotation.y` は直接回されるのに
`visualFacing` は入力時のまま固定されるので、その差を「見ている向きとの
ずれ」として食わせると首と腰が毎フレーム可動域いっぱいまで振り切れる。

#### 将来の判断待ち項目との整合(確認のみ)

- **オート連撃を入れてもモーションは破綻しない。** 必殺技中に通常攻撃を
  入力しても `beginMove()` がクリップを上書きし、`state.ultHitFrac` は
  そこで 0 に戻る。保留中の必殺技は予定どおり解決する。
- **武器切替とリキャストの共通化は既に成立している。** 必殺技の発動条件は
  ゲージ(`ULT_GAUGE_MAX`)と 1.5 秒ロックアウトだけで、`ult.cd` は
  どこからも読まれていない。型を選ぶ `ultClipName()` も装備ではなく
  転身で分岐するので、D-pad 武器切替を入れても踏み倒せない。

### 4-7. Combat Idle の構造的欠陥 2 件の修正

Combat Idle を完成状態か点検した結果、職業ごとの味付け以前に
**機械的に誤りと言える欠陥が 2 件**見つかったので、そこだけ直した。

#### ① 腰の横移動が fps 依存で、設計値の 3〜13 倍になっていた

`updateLocomotion` は `waist.position.x` を「目標値へ毎フレーム寄せる」
形(収束率 `dt*12`)で動かしている。Combat Idle の重心移動を、その lerp の
**後で** `position.x` へ直接足していたのが原因。足した分は次のフレームで
`1 - dt*12` しか戻らないので、収束率で割った分だけ積み上がる。

```
旧: 30fps 3.4cm / 60fps 6.0cm / 144fps 13.4cm   ばらつき 3.91 倍
新: 30fps 1.8cm / 60fps 1.8cm / 144fps 1.8cm    設計値 1.8cm
```

直し方は「加算をやめて目標値の側へ合成する」。`stepWaistShift()` に
まとめ、`idleShift` が 0 のときは置き換える前の式と**完全に一致**する
(移動中の既存モーションは変わらない)。60fps の見た目は以前より
小さくなるが、6.0cm の方がバグだった。

#### ② バーサーカーの「常時前傾＋低い構え」が Combat Idle 中だけ消えていた

`updateLocomotion` が毎フレーム書いている `jobPitchBias 0.10` と
`jobKneeBias 0.20` を、Combat Idle が構えを**絶対値で**当てることで
上書きしていた。沈み込み(`crouch` 5.4cm)だけは効いたままなので、
**沈むのに膝が伸びる**という矛盾した姿勢になっていた。

これは同じ事故の三度目にあたる。もともと `applyJobPromotionVisual` 側で
一度だけ書いていたものが歩行に上書きされて消え(一度目)、歩行側へ
移して直し(二度目の修正)、その上へ Combat Idle が乗って再発した。
そこで**値の出どころを `JOB_POSTURE_BIAS` 一箇所に集約**し、歩行も
Combat Idle も同じ表を読むようにした。値そのものは移設前と同一。

#### 純粋関数化

ターゲットの組み立てを `buildCombatIdleTarget()` に集約し、
`applyCombatIdlePose()` はそれを呼ぶだけにした。テストが
「本番と同じ手順で作ったターゲット」を検査できるようにするため ――
テスト側で組み立てを書き写すと、片方だけ直して気づかない。

#### テストで固定したこと

現在の数値を正しいものとして固定するのではなく、**見つかった 2 件が
再発しないこと**だけを見る(`tests/unit/combat-stance.test.js`):

- 腰の横移動が 30 / 60 / 144fps で振幅 5% 以内に収まる(①)
- 収束率が dt に対して正規化されている(①)
- `idleShift = 0` のとき置き換え前の式と厳密一致(①の副作用防止)
- `jobPitchBias` / `jobKneeBias` が Combat Idle 適用後も残る(②)
- 沈み込みと膝の向きが矛盾しない(②の実際の見え方)
- ウェイト 1 でポーズを当て切った後もバイアスが残る(②の本番経路)
- バイアスを持たない職は一切変わらない

#### 戦騎士の仕様判断

指示の「剣士より軽快」は、既存リポジトリの battleKnight 像
(`JOB_ATTACK_TEMPO 1.25`「重量感」、`JOB_IDLE_MUL` の「重い。ゆったり」)と
正面から衝突していた。**Combat Idle は重厚のまま維持**し、剣士との差は
攻撃モーション側(直線的・効率的)で付ける方針に決定。今回は係数を
変更していない。

#### 今回やっていないこと(B 判定、別フェーズ)

弓師・鷹の目の `weapon` 係数、バーサーカーの `breath` 係数、呼吸の
二重化(胸郭 `0.62` 固定 vs 腰 `rate`)、胸郭チャンネルの追加、
頭部と武器向きの Combat Idle 連動、脚を重心移動へ追従させる改修。

---

## PHASE 5. 優先順位と実装状況

### 5-1. 今回実装(優先度S)

| # | 項目 | 状態 |
|---|---|---|
| S-1 | 遮蔽物による視界制限(xrayシェル・ミニマップ) | **実装** |
| S-2 | 高速連撃を受け止められるモーション基盤 | **確認のみ**(既に成立。次の `beginMove()` が前のクリップを上書きする) |
| S-3 | Combat Idle + 振り終わりの settle + 職業別の重心 | **実装** |
| S-4 | 処刑システムの基盤 | **実装** |
| S-5 | カメラ操作・探索視点 | **保留**(PHASE 2-1 の 3 番。入力設計の合意待ち) |
| S-6 | 敵の視認/戦闘時ハイライト | **実装** |
| A-2 | ネームド方向マーキング(◆ ネームド反応 → 北東) | **実装**(効果が高く既存への影響が小さいので前倒しした) |
| A-7 | Eye Rig / Visual Look Offset(4-4) | **実装** |
| A-4 | 武器別必殺技(サブ武器、4-5) | **実装** |
| A-5 | 共通リキャスト | **既に成立**(`ult.cd` は未使用、ゲージ制) |
| A-6 | 敵配置の密度調整 | **実装せず**。前提が違っていた(4-5)。代わりに「気配」を実装 |
| ― | メイン武器の必殺技モーション(8職ぶん、4-6) | **実装** |
| ― | 必殺技の判定・SE・見た目の同期(4-6) | **実装** |
| ― | 演出の階層(通常ヒット < 必殺技 < 処刑、4-6) | **実装** |

### 4-5. 武器別必殺技(A-4)と「気配」(A-6の再定義)

#### 武器別必殺技 ―― サブ武器にだけ与える

必殺技には既に3つの出どころがある(基礎職の `ult` / スフィア盤で解放する
別の型 `ULT_ALT_BY_CLASS` / 上位職専用 `JOB_ULT_BY_JOB`)。ここへ
「武器で決まる必殺技」を無条件に被せると、プレイヤーがスフィアを割いて
選んだ型も、転身で手に入れた到達点も、武器を持ち替えた瞬間に消える。

一方でメイン武器側の必殺技は、資料が挙げている姿と既に一致している
(剣士=大きく踏み込む強斬撃=渾身の斬撃、弓師=素早い連射=八方の矢、
鷹の目=大きく薙ぐ=天翔ける鏃)。**足りていないのはサブ武器の方**で、
槍・刀・魔法の剣・ボウガンへ持ち替えても大剣/双剣/杖/小弓の必殺技が
そのまま出ていた。

| サブ武器 | 必殺技 | 型 |
|---|---|---|
| 槍 | 烈風の一突き | 前方へ深く踏み込む単発(`reach:5.2`、範囲は絞る) |
| 刀 | 月影・二段 | 2段(`hits:2`)。踏み込んで斬り、返しで斬る |
| 魔法の剣 | 星霜斬 | 近接3連(`hits:3`)。杖の遠隔メテオに対する選択肢 |
| ボウガン | 貫穿の一矢 | 溜めて撃つ(`aimed`)。小弓の連射に対する一撃 |

数値(`mult`/`radius`)は今の段(基礎/スフィア/上位職)から倍率で引き継ぐ
ので、転身して強くなった実感は持ち替えても消えない。多段は総ダメージを
分割するだけなので DPS は変わらない ―― 変わるのは手触りだけ。

**リキャストは元から共通だった。** `ult.cd` はどこからも読まれておらず、
発動条件は必殺ゲージ(`ULT_GAUGE_MAX`)と発動直後の 1.5 秒ロックアウトだけ。
武器を持ち替えてゲージを踏み倒すことはできないので、ここは何も変えていない。

持ち替えた瞬間に `⚡ 必殺技「…」` のトーストを出す ―― HUDのアイコンが
黙って入れ替わるだけでは「今この必殺技を使いたいから武器を変更する」の
判断材料にならないため。

#### 「気配」―― 敵配置を間引く代わりにやったこと

当初 A-6 は「森・廃村で敵を間引いて探索のリズムを作る」と書いた。
**調べた結果その前提が違っていたので、実装しなかった。**

- 最初のメインシナリオ(囚われの洋館)には**徘徊する常設敵が1体もいない**。
  戦うのはシナリオ上の5点だけで、森と使用人区画はイベントで湧く。
  この5戦は学習曲線として設計されている(`COMBAT_DESIGN.md` 9章)。
- 他のダンジョンも座標直書きの手配置で、封鎖部屋・中ボス・ゲートと
  結びついている。数を減らすと `gateTag` の進行そのものが変わる。

つまり「敵が多すぎて常時戦闘」という状態ではなく、間引くと設計済みの
学習曲線を壊すだけだった。

代わりに、視界制限を入れたことで空いた穴を塞いだ。壁の向こうの敵を
隠したのに、隠した先に手掛かりが何も無ければ「壁の向こうに敵がいるかも
しれない」ではなく、ただ「何も無い」になる。そこで **近くにいて・まだ
こちらに気づいておらず・今は見えていない敵だけ**が、ごくたまに足元の
土煙と足音を漏らすようにした:

- **音** … 方向は分からないが「近くで何かが動いた」ことは伝わる
- **土煙** … 壁の向こうなら壁に隠れる。回り込む/覗き込むと見える

つまり「回り込む・壁際から覗く」という行動そのものへの報酬になる。
鳴らしすぎれば雑音になるので、個体ごとの間隔に加えて全体でも1つずつしか
鳴らない予算を持たせてある。交戦が始まった敵は既に自分の足音と攻撃音を
持っているので対象外。

---

### 5-2. 次にやるべきこと(優先度A、順序付き)

1. **戦闘テンポの数値合わせ**。順序が大事:
   ① ヒットリアクション・SE・カメラの細分化(体感の作り込み)
   → ② 攻撃間隔の短縮(`atkCooldown` とクリップ長を同時に縮める)
   → ③ **最後に**敵HP。①②を飛ばしてHPだけ上げると、要求が明確に
   禁じている「硬い」だけの敵になる。現状は単発119に対し雑魚HP58〜190で
   1〜2発、要求は10発以上。**4〜8倍のHP変更は影響が大きいので、
   ①②を終えてから一括で行うべきで、今回は触っていない。**
2. **優先度B**(下記 PHASE 6 の調査結果に沿って、強モブ → 高難易度 →
   マップ変異 → 光演出 の順)。
3. **サブ武器の必殺技モーション**。メイン武器は専用の型を書き起こしたが、
   サブ武器(槍・刀・魔法の剣)は通常攻撃の型の流用のまま。

### 5-3. 保留(判断が要るもの)

- 攻撃ボタン長押しの自動連撃(溜め技と衝突)
- 十字キーの武器切り替え(4方向すべて使用済み)
- 右スティックの役割変更(職業アイデンティティと衝突)
- 敵HPの大幅増(上記の順序どおり最後に)

### 5-4. 優先度B(未着手)

強モブ、マップ変異、高難易度化、木漏れ日/光の筋といった高度な光演出、
細かな環境リアクション。


---

## PHASE 6. 優先度B の調査(実装前の切り分け)

「全部を一度に実装しない」という方針どおり、まずリポジトリを読んで
**既存システムで賄えるもの / 新規が要るもの** に分けた。結論から言うと、
4項目のうち**マップ変異は既に半分実装済み**で、**強モブはフラグだけあって
中身が無い**。順序は後述のとおり入れ替えを推奨する。

### 6-A. 強モブ ―― 「フラグはあるが強くない」

`strongMob` は既に敵データにあるが、実際に効いているのは次だけ:

| 効いていること | 場所 |
|---|---|
| 見た目が 1.5 倍 | `buildEnemy` |
| HPバー/体幹バーが elite 表示 | `14-hud-boot.js` |
| ノックバックが小さい(0.16 / 通常 0.32) | `dealDamageToEnemy` |
| 死亡演出が長く、カメラ揺れが少し大きい | `finishEnemyDeath` |
| 撃破時に 25% で追加装備ドロップ | `finishEnemyDeath` |
| 体幹が 130(通常 55) | `core/stagger-math.js` |

つまり**特殊攻撃も、攻撃パターンの違いも、スーパーアーマーも、専用
ドロップも無い**。「単純にHPが高い大きい敵」そのもの。

- **再利用できる**: `strongMob` フラグ、elite HPバー、ドロップのフック、
  体幹システム、`guardian`(常時ガード)、`chargeTelegraphOverride`
  (溜めの長さ)、`chargeCooldownOverride`(攻撃間隔)、`turret`、`flying`、
  `midbossName`。型の材料は既に全部ある。
- **新規が要る**: ①スーパーアーマー(体幹が尽きるまで怯まない)
  ②特殊攻撃のスケジューリング ③専用ドロップテーブル。

`chargeTelegraphOverride` / `chargeCooldownOverride` が既にあるので、
「予兆が長い代わりに一撃が重い」「攻撃間隔が短い」といった差別化は
**データだけで書ける**。見分けがつかなくなる複雑化を避けるという方針にも合う。

### 6-B. 高難易度 ―― 「まさに硬くなるだけ」

`difficultyFor(key)` が単一の合流点で、★(= クリア回数)から

```js
{ hp: (1 + t*0.55) * 1.20, atk: (1 + t*0.28) * 1.10, speed: 1 + t*0.06,
  xp: 1 + t*0.34, gold: 1 + t*0.30 }
```

を返すだけ。指示が明確に避けたい「敵がただ硬くなる」状態そのもの。

- **再利用できる**: `difficultyFor` が唯一の入口なので、ここへ軸を足せば
  全ダンジョンへ一度に効く。敵の攻撃間隔も `en.atkCdBase` という
  上書きフックが既にある(既定 1.6〜1.8 秒)。
- **新規が要る**: `difficultyFor` の戻り値に軸を追加(攻撃間隔倍率・
  強モブ率・追加配置数・視界距離)し、`buildEnemy` / `spawnEnemies` /
  `core/enemy-visibility.js` の `sightRange` へ配る。HP倍率はむしろ
  **下げて**、他の軸へ振り替えるのが筋。

### 6-C. マップ変異 ―― 「既に半分ある」

これは調べて分かった一番大きいこと。**★で構造が変わる仕組みは既にある**:

| 既存の仕組み | 内容 |
|---|---|
| `MANSION_CRYPT_DEPTHS_STARS = 3` | 地下納骨堂の最奥が★3で開く |
| `MANSION_ATTIC_STARS = 4` | ボスの先、屋根裏が★4で開く |
| `TEMPLE_DEPTHS_STARS = 4` / `CONSERVATORY_DEPTHS_STARS = 4` / `TOWER_HOUSE1_DEPTHS_STARS = 3` | 同様の継ぎ足し |
| `routeMutationActive(scKey, nodeKey)` | ★4以上でルートグラフのノードを差し替える枠組み(現在 `ROUTE_MUTATABLE_NODES` が空で休止中) |
| `spawnTaggedGroup(tag, defs)` | 任意の場所へ一括で敵を湧かせる(森の待ち伏せ等で稼働中) |

- **再利用できる**: 上記すべて。特に `routeMutationActive` は「仕組みは
  残してある」と明記された**空のまま生きている枠**で、ここへノードを
  登録し直すだけで通路の付け替えが動く。
- **新規が要る**: 周回ごとに決まる**決定的なシード**(同じ★なら毎回同じ
  配置になり、覚えて攻略できる)と、そのシードで敵配置・宝箱・ネームド
  出現地点を選ぶ薄い層。座標直書きのテーブルは触らず、「どのグループを
  出すか」の選択だけを変えるのが最小変更。

### 6-D. 高度な光演出 ―― 「土台は十分、足りないのは階層」

- **再利用できる**: `WORLD_MOOD`(ワールドごとの sky/fog/sun/hemi/rim/
  露出)、`ALTITUDE_BANDS`(時計塔の高度で空が変わる)、
  `toneMappingExposure`、`flashScreen`、`spawnHitSpark`、
  今回入れた `threatHighlight`(敵の可視段階と連動した発光)。
- **新規が要る**: 実はほとんど無い。今回の必殺技改修で
  **通常ヒット < 必殺技 < 処刑** の階層は入った。残っているのは
  「探索中の控えめな光」と「敵発見の小さな変化」「ネームドの存在感」で、
  これは `WORLD_MOOD` の値と `threatHighlight` の係数の調整が主。
  木漏れ日・光の筋のような**新しい描画は、見た目の割にコストが高い**
  (この見下ろしカメラでは効果が出にくいことが Face 再設計フェーズの
  検証で既に分かっている)ので、優先度は最後で構わない。

### 6-E. 推奨する順序(指示の A→D から変更)

1. **強モブ(6-A)** ―― 材料が全部揃っていて、「敵を発見する → 戦う」の
   質に直結する。スーパーアーマーと攻撃間隔だけでも体験が変わる。
2. **高難易度(6-B)** ―― 合流点が1つなので、強モブ率を軸に足せば
   1と2が同じ変更で繋がる。
3. **マップ変異(6-C)** ―― 既存の★ゲートと `routeMutationActive` の
   再稼働から。新規シード層はその後。
4. **高度な光演出(6-D)** ―― 階層の骨格は今回入ったので、残りは調整。

**この順序を薦める理由**: 1 と 2 は `difficultyFor` と `buildEnemy` という
同じ合流点を共有するので、続けてやると変更が1箇所で済む。逆に 3 を先に
やると、変異した配置に対して「どの敵を強モブにするか」を後から足すことに
なり、同じ場所を二度触る。
