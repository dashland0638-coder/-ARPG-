# 戦闘・職業システム設計ノート

このファイルは、「敵を見ることでプレイヤーが有利になるARPG」という設計思想
(2026-09の設計引き継ぎ指示)に沿って戦闘・職業システムを整理した際の記録。
シナリオ・ダンジョン構造・イベント進行には一切触れていない(それらは別チャットの
担当)。以降の作業はここに追記していく想定。

## 0. 最重要コンセプト(踏襲)

敵を見る → 行動を理解する → 位置を選ぶ → タイミングよく攻撃する →
敵の体勢・向きを崩す → 有利な状況を作る、という基本ループを、単純な
「レベルを上げる/連打する」の上位互換にしない。職業ごとに「何を見るか」を
変える(剣士=距離、戦騎士=身体、盗賊=隙間、バーサーカー=次の位置、
弓師=現在、鷹の目=未来、魔法使い=戦場、魔導士=戦場の変化)。

## 1. 解析結果 - 実装済み・素の状態(2026-09時点)

大量の作業が既に済んでいたため、今回はゼロからの設計ではなく **差分の実装**
になっている。解析で分かったこと:

- **職業構成は既に指示通り**: 基本職(剣士/盗賊/魔法使い/弓師)→上位職
  (戦騎士/バーサーカー/魔導士/鷹の目)の対応、`state.job` フラグ方式、
  上位職専用の見た目・武器モデル(斧/大弓等)・専用スキル1種
  (`unlockKey:'job'`)・専用必殺技(`JOB_ULT_BY_JOB`)まで実装済み
  (`src/legacy/parts/01-character-creation.js`, `06-player-enemy.js`,
  `12-progression-ui.js`)。
- **体幹(Stagger)システムは既にある**: `en.posture`/`en.postureMax`、
  ダウン(`triggerKnockdown`)、大怯み閾値(70%)、クラスごとの
  `staggerMul`、技ごとの`opts.staggerMul`ボーナス(回避攻撃・ジャンプ攻撃・
  各種スキル)まで実装済み(`07-ai-combat.js`)。
- **ジャストドッジ/バリア(パリィ)も既にある**: `state.dodging`中の被弾を
  検出して反撃ダメージ+50%の窓を開く仕組み、および全職業共通の
  「バリア」スキル(構えて弾く+HP吸収、発動中無敵)が既に存在。
- **職業ごとの差別化も基礎はある**: コンボ段数・型がクラスごとに違う
  (大剣3段/双剣4段/杖2段/弓4段)、弓師は近接距離でダメージ-35%、
  盗賊の刀は1段目確定クリティカル、等(`core/damage-math.js`)。

一方で、今回の設計書が明確に「してはいけない」と書いている状態が
そのまま残っていた箇所があった:

- **敵・ボスの向きが毎フレーム瞬間スナップ**
  (`en.group.rotation.y = Math.atan2(...)`)。回転速度の概念が一切なく、
  design #21/#22「瞬時に180度回転してはいけない」に反する。
- **上位職の必殺技/専用スキルが「同じ判定の数値違い」であることが
  コード自体にコメントで明記**されていた
  (`12-progression-ui.js`: 「新しい当たり判定の形は増やさず…数値だけを
  大きく強化してある」)。design #4/#6/#9/#13/#14 が禁止する
  「単純な上位互換」そのもの。
- **攻撃後の「敵の身体の流れ」を利用する仕組みが無い**
  (design #23-26)。ボスは振り終わったら即座に平常運転(=即座に
  プレイヤーへ向き直る)に戻っていた。
- **通常攻撃が完全に足を止めた状態でしか出せない**
  (バーサーカーの「攻撃モーションが移動になる」design #11 が未実装)。
- **鷹の目の「予兆を読んで未来の位置を狙う」が数値上の名前だけ**
  で、実際のロジックは他職の同型スキルと同一だった(design #15-18)。

## 2. 今回実装した差分

既存を壊さないことを最優先し、**新しいボタン/UI/システムを増やさず**、
既存の仕組み(体幹、バリア、スキルのチャージ、`state.skillAnim`の移動演出)
に薄く乗せる形で実装した。

### 2-1. 敵・ボスの向き(`src/core/enemy-facing.js`)

`turnTowardAngle(current, target, maxDelta)` という純粋関数を切り出し
(`tests/unit/enemy-facing.test.js`)、以下のAIの「毎フレーム瞬間スナップ」を
「毎フレーム、旋回速度の分だけ向き直る」に置き換えた:
`updateWanderAI` / `updateFireEnemyAI` / `updateKiteAI` / `updateTurretAI` /
`updateBossAI`(通常の追跡中)。

**変えていない箇所**: 突進(`updateChargerAI`)のダッシュ方向、ジャンプ
(`updateJumperAI`)の空中方向、ボスの振りかぶり中(`atkWindup`)の向き -
これらは「攻撃に踏み切った瞬間、向きが一気に定まる」こと自体が予兆に
なるので、意図的に瞬間ロックのまま残した(design #44完了条件
「攻撃中は必要に応じて方向固定される」)。

旋回速度は雑魚(`DEFAULT_MOB_TURN_RATE`)とボス(`DEFAULT_BOSS_TURN_RATE`、
雑魚の約1/3)で分け、個々の敵に`en.turnRate`があればそちらを優先する
(将来のボス別チューニング用のフック)。

### 2-2. ボスの「攻撃後の身体の流れ」(`updateBossAI`)

ボスの振りかぶりが終わった瞬間(命中・空振り問わず)、`en.postAtkRecoveryT`
(0.45秒)を開始。この間はボスの体がまだ攻撃方向へわずかに流れ続け
(`en.postAtkDriftDir`)、向き直りもこの間だけさらに遅くする。design
#23-26「攻撃後の身体の流れを利用できる」の最小実装。

### 2-3. パニッシュ窓(全職業共通、`src/core/stagger-math.js`)

`dealDamageToEnemy`が体幹ダメージを計算する際、対象が
**振りかぶり中(`en.atkWindup`)** または **攻撃直後の隙
(`en.postAtkRecoveryT>0`)** であれば、体幹ダメージに1.6倍/1.3倍の
ボーナスが乗るようにした(`punishWindowMultiplier`)。特定の技や職業に
紐付けず、**全クラス共通**にしてある - 「敵を見て隙を突く」という設計の
中核ループ自体への見返りだから。命中時は既存のヒットスパークとは別に
専用SEを1つ足し、「今の一撃は違う」と分かるようにした。

### 2-4. 戦騎士: Perfect Brace(`tryPerfectDodge`)

新しい入力は増やさず、既存の全職共通「バリア」スキル(構えて弾く)に
`state.job==='battleKnight'`の場合だけ追加効果を乗せた:

- 攻撃元の敵の体幹を大きく削る(既存のstagger-math.jsをそのまま再利用)。
- `state.braceCounterT`(3秒)の反撃猶予を開き、次の一撃に+40%
  (ジャストドッジの+50%より控えめ、かつクリティカル確定ではない -
  `core/damage-math.js`の`braceCounterOpen`)。

「敵の攻撃を誘う→見る→受け流す→反撃」という design #6/#6-1 の流れを、
既存の何もかもを壊さずに一番小さい差分で成立させてある。単純な
スーパーアーマーは付与していない(構え自体は既存バリアのまま:
発動中は無敵、外すとただの硬直)。

### 2-5. バーサーカー: 攻撃と移動の連結(`tryAttack`)

`state.job==='berserker'`の場合、通常コンボの一振りごとに、入力方向
(押していなければ現在の向き)へ短い踏み込み(`state.skillAnim`、
既存のスキル/溜め技の移動演出をそのまま再利用)を自動で追加した。
フィニッシュはやや長め(2.6)、それ以外は短め(1.7)。ヒット判定自体は
振った瞬間に解決される(既存の仕様のまま)ので、この移動は
「次の一撃のための足運び」専用になる。新しい無敵/スーパーアーマーは
一切付与していない(design #12/#41で明示的に禁止)。

### 2-6. 鷹の目: 予兆を読んだ狙い筋補正(`executeVariant`のline系スキル)

「貫通の一矢」「天賦の一矢」は貫通即着弾(飛翔時間が無い)ため、
矢を誘導する代わりに **狙い筋そのもの** を予兆に応じて補正する形にした
(`src/core/predictive-aim.js`: `telegraphLead`/`predictLeadPosition`)。

`state.job==='hawkEye'`の時だけ、正面のゆるいコーン内に
既存AIが追える予兆状態(突進の踏み込み、跳躍の滞空)の敵がいれば、
**チャージした時間の分だけ**その敵の未来位置を読んで狙い筋を寄せる
(長く引くほど先まで読む = design #18「チャージ中を待ち時間にしない」)。
命中時、対象がまだ予兆状態のままなら体幹ボーナス(`staggerMul:3.0`)と
専用トースト/SEを出す。対象が予兆状態でなければ通常通り(ボーナス無し)。
自動追尾ではない - 元々狙っていない方向の敵は拾わない。

### 2-7. 魔導士: 命中で足取りを鈍らせる(`dealDamageToEnemy`)

「戦場そのものを変える」の最小実装として、`state.job==='archmage'`の
命中1つ1つに`en.arcaneBindT`(2.2秒)を付与し、その間は該当の敵の
旋回速度(`enemy-facing.js`の`resolveTurnRate`が`en.turnRateMul`を参照)と
ボスの追跡速度を0.5倍に落とす。敵側に新しい状態機械を増やさず、
既存の参照点に薄い乗数を足しただけ。

## 3. あえて今回やらなかったこと(将来の拡張候補)

- **鷹の目の必殺技(天翔ける鏃)は据え置き**。現状`radial+sweep`(旋回して
  周囲へ矢を放つ)であり、`aimed`型(魔法使い/魔導士/盗賊/バーサーカーの
  必殺技はこちら)ではないため、2-6の予兆読みはスキル(貫通の一矢/
  天賦の一矢)側に実装した。必殺技自体を`aimed`型へ作り替えるのは
  既存のバランス・演出への影響が大きく、今回は見送った。
- **魔導士の設置型フィールド制御**(design #20の「魔法の設置」「戦場に
  残る効果」)は2-7の一時debuffに留めた。地面に残る持続エリア等は
  範囲が大きいため別途の実装フェーズを推奨。
- **ボスの視線(gaze)を予兆に使う**(design #26)は未着手。既存の首(neck)
  yaw追従はプレイヤーを向く方向にしか使われておらず、「あえて外す」
  演出には新しい状態を要するため見送った。
- **基本4職(剣士/盗賊/魔法使い/弓師)自体の再設計はしていない** - 解析の
  結果、距離依存ダメージ・コンボ型・武器思想は既に design の意図と
  一致していたため、上位職側の差分実装を優先した。
- 雑魚敵(charger/kiteなど)の移動速度自体への`arcaneBindT`適用は
  ボスの追跡速度にしか配線していない。トラッシュ敵にも広げるのは
  小さな追加作業。

## 4. 自動テスト

`npm run test:unit`(node:test)に以下を追加:

- `tests/unit/enemy-facing.test.js` - 角度の正規化・旋回クランプ・
  ボス/雑魚のデフォルト旋回速度。
- `tests/unit/stagger-math.test.js` - パニッシュ窓の倍率、体幹加算の
  クランプ、大怯み/ダウン閾値。
- `tests/unit/predictive-aim.test.js` - 予兆判定、突進/跳躍からの
  未来位置の外挿、遅延(踏み込み中)の扱い。
- `tests/unit/damage-math.test.js`に`braceCounterOpen`のケースを追加。
- `tests/unit/melee-hit.test.js` - 敵の表面基準の近接判定(第2次)。
- `tests/unit/enemy-step.test.js` - Enemy Stepの発動条件(第2次)。
- `tests/unit/swing-timing.test.js` - 攻撃SE/Hit判定/見た目の刃が
  同じ瞬間を指すこと、遅延の適用範囲(第3次 / 7-1)。
- `tests/unit/soft-lock.test.js` - バーサーカーのソフトロック取得・保持・
  解除。特に「後ろへ下がっても保持される」こと(第3次 / 7-2)。
- `tests/unit/uppercut.test.js` - 上昇/落下の分岐、重量クラス、浮かせ量、
  および「切り上げ連打が通常攻撃より速くならない」体幹バランス
  (第3次 / 7-5, 7-6)。

E2E(`npm run test`、Playwright):

- `tests/combat-test-arena.spec.js` - Arenaのロスター全種のspawn/Clear。
- `tests/air-actions.spec.js` - 上昇中の攻撃が切り上げに、落下中の攻撃が
  落下攻撃になること / 空中回避と空中スキルが弾かれること /
  Enemy Stepが維持されていること / Test Modeが通常セーブへ漏れないこと。

空中アクションのE2Eでは待ち時間で滞空の位相を決め打ちしていない ――
`animate()`の`dt`は1フレーム0.05秒で頭打ちなので、ヘッドレスで
フレームが詰まるとゲーム内時間が実時間より遅れる。落下中の確認は
待ちを伸ばしながら試し、Enemy Stepの確認はジャンプ間隔をずらして
突進周期との位相を掃かせている(固定間隔では位相ロックして落ちた)。

既存の`npm run test`(Playwright、`tests/save-load.spec.js`の
sortieケース)で、上記変更を含めた状態でダンジョンに入り実際に
攻撃してもconsole error/pageerrorが出ないことを確認済み。

## 5. Combat Design Audit(2026-09、監査と回収)

上記フェーズ完了後、「コードとして存在する」≠「プレイヤーがゲームとして
体験できる」という観点で全体を再監査した。監査の結論は以下の通り
(詳細はセッションのやり取りに残っているため、ここでは実装差分のみ記録):

- パニッシュ窓・Perfect Brace・鷹の目の予測・魔導士の鈍化は、いずれも
  「コードは動くが、非デフォルトのスキル選択が前提」「フィードバックが
  無い」等の理由で、通常プレイではまず体験されない状態だった。
- バーサーカーの攻撃移動(前フェーズで実装)は動作するが、直進ダッシュの
  ため敵を素通りしてしまい、設計意図(位置関係を変え続ける)と乖離していた。
- エネミーステップ・切り上げ攻撃・落下中の切り下げ攻撃は、Git履歴・
  コード・本ドキュメントのいずれにも痕跡が無く、このリポジトリ単独では
  設計意図を再構築できないと判断。**今回も実装していない**(要ユーザー確認)。

### 5-1. Combat Test Arena(新規)

テストモード(タイトル→🛠テストモード→'training'ワールド)専用の
検証環境。目的は敵コンテンツの追加ではなく「戦闘システムを意図的に
発動・確認できる場」であること。

- `src/legacy/parts/07-ai-combat.js`: `ARENA_ROSTER`(Dummy/Basic Melee/
  Windup Enemy/Charge Enemy/Jump Enemy/Boss Test)と`arenaSpawn()`/
  `arenaClear()`/`arenaCycleSpawn()`。既存の`buildEnemy`/`buildBoss`と
  既存atkType(charge/jumper/passive)をそのまま流用し、新しい敵AIは
  追加していない。Boss Testは`mansionBoss`をhpMax:50000で流用 ――
  通常の撃破報酬フロー(3択報酬・勝利演出)を実用上発火させないための
  措置で、旋回・攻撃後の流れ・体幹・Perfect Braceの検証が目的であり
  撃破自体は目的ではないため。
  - `updateChargerAI`に`en.chargeTelegraphOverride`を追加(未指定なら
    従来通り0.65秒) ―― Windup Enemyだけ振りかぶりを長く見せるための
    最小限の変更で、既存の突進系モブの挙動は変えていない。
- `src/legacy/parts/14-training-ground.js`: パネルの開閉・ロスター
  ボタン描画・Debug Feedbackログ(`emitArenaFeedback`)・敵情報パネル
  (`updateArenaEnemyInfo`)。すべて`state.testMode`時のみ表示され、
  通常プレイのUIには一切影響しない。
- 操作(#10): タップ/クリックの実DOMボタンが主(iPhone想定)。コントローラーは
  ゲームプレイ中未使用だったD-pad右(`btnPressed(gp,15)`)1つで
  ロスターを巡回スポーンできるようにした(パネル自体は`state.paused`を
  立てない設計にしてあり、戦闘を止めずに検証できることを優先したため、
  既存のポーズ専用ゲームパッドメニューナビ(10-input.js)には乗せていない)。
- Debug Feedback: WINDUP PUNISH/RECOVERY PUNISH(実際の倍率つき)、
  PERFECT BRACE+COUNTER WINDOW(残り秒数)、PREDICTIVE AIM/PREDICTIVE HIT、
  TURN SLOW(残り秒数)を`emitArenaFeedback()`経由で表示。呼び出し側は
  `state.testMode`を意識せず呼べる(関数内で一括ガード)。

### 5-2. バーサーカー攻撃移動の再設計(※6-4で撤回済み)

> この節の接線方向移動は、実プレイ評価を受けて**6-4で撤去**されている
> (`src/core/flank-step.js`ごと削除)。以下は当時の記録として残す。

`src/core/flank-step.js`(新規、`tests/unit/flank-step.test.js`で検証)。
直進ダッシュを、直近の交戦相手(6m以内)への相対位置(接線方向)を
基準にした移動へ差し替えた。プレイヤーの入力方向で「どちら側へ回り込むか」
を選べる(無入力時はコンボ段数で左右を綾織りにして固定化を避ける)。
`clampStepDistance()`で、近距離での踏み込みが相手の位置を素通りしないよう
distanceを制限する。自動追尾・無敵・スーパーアーマーは追加していない。

### 5-3. 戦騎士のデフォルト戦闘体験化

Perfect Brace(体幹を崩す+反撃猶予)の発火条件から「バリア選択」の
必須を外した。既存のジャストドッジ(全職業共通、ダッジボタンのタイミング
合わせ)自体が、`state.job==='battleKnight'`の時だけ受け流し(Brace)に
差し替わる ―― 新しい入力は増やさず、動きも変えていない(転がって
逃げるモーションはそのまま)。バリア経由のPerfect Brace(既存)は、より
強い代替手段として引き続き利用できる。`applyBattleKnightBrace()`に
共通処理を切り出した。

### 5-4. 鷹の目のデフォルト戦闘体験化

通常の弓矢(`spawnProjectileSingle`)自体に予測補正を組み込んだ。
`state.job==='hawkEye'`かつ正面のゆるいコーン内に予兆状態の敵がいれば、
矢の飛翔時間(距離÷矢速)を先読み時間として着弾予測位置へ狙い筋を
一度だけ曲げる。矢はhomingにしていないため、その後の相手の行動次第で
普通に外れうる(自動追尾ではない)。命中時、対象が引き続き予兆状態
だった場合のみ体幹ボーナス+専用演出。スキル(貫通の一矢/天賦の一矢)
側の既存実装は、より強い代替手段として維持。

### 5-5. 魔導士の体感フィードバック

`en.arcaneBindT`が新規に付与された瞬間(既にかかっている間の連打では
再発火しない)、対象の足元に短命の魔法陣(`spawnScorch`流用、専用色)+
専用SEを追加。「内部処理のみで気づけない」を解消する最小実装。

## 6. Combat Feel / Stagger監査 / 空中アクション(2026-09、第2次監査)

実プレイのフィードバックを受けた再監査と修正。

### 6-1. 体幹(Stagger)監査 ― ボスゲージが動かなかった原因

ボスHP下のゲージは**Staggerゲージそのもの**で、UIも`boss.posture/postureMax`に
正しく繋がっていた。動かなかったのは数値設計の破綻:

- 自然減衰が `postureMax × 0.35 /秒` と**上限に比例**していた
- 一方、獲得は `BASE_STAGGER_GAIN=10` 基準の**固定値**
- ボスの`postureMax`は`hpMax*0.28`(HP由来)なので、HPが増えるほど減衰だけが重くなる

結果、減衰 61〜255/秒 に対し獲得は最大でも約45/秒で、**全ボスで原理的に一度も溜まらなかった**。
盗賊(`staggerMul:0.7`)は通常敵でも減衰とほぼ拮抗しており、崩せない職になっていた。

修正(`src/core/stagger-math.js`):
- 減衰を**割合から絶対量へ**(通常敵16/秒・ボス12/秒)
- ボスの`postureMax`を`bossPostureMax()`で180〜320に頭打ち(HPインフレと切り離す)
- 「通常攻撃だけでも崩せるが時間がかかる/パニッシュ・受け流し・Enemy Stepで一気に短縮」
  という関係をユニットテストで固定した

### 6-2. 近接判定 ― 「見た目では当たっているのにHitしない」の原因

`findMeleeTarget()`系が**プレイヤー中心→敵の原点(足元)**の距離と角度だけを見ており、
敵の体の大きさを完全に無視していた。胴体が武器に重なって見えていても原点が射程外なら
必ず外れ、至近距離では角度誤差が爆発して密着した敵が扇から外れていた。

修正(`src/core/melee-hit.js`): 判定基準を**敵の表面**へ変更。
- 射程は「原点までの距離 − 敵の半径」で測る
- 角度は「敵の体が占める角度幅」ぶん許容を広げる
- 半径0を渡せば旧挙動と完全一致(職業ごとの`meleeRange`/`meleeAngle`は不変)

半径は雑魚がbodyメッシュの実測、ボスは`solidR`(押し出し半径=プレイヤーが
近づける限界)を流用している。

### 6-3. 戦騎士のHitタイミング

判定が入力フレームに同期していたため「判定→Hit→その後に剣が振られる」と見えていた。
`JOB_HIT_DELAY_FRAC`(戦騎士のみ0.45)を追加し、ダメージ判定と範囲表示だけを
クリップ長の45%(約0.20秒)まで遅らせる。モーション開始・SE・コンボ受付は
従来どおり即座なので入力レスポンスは変わらない。あわせて`JOB_MELEE_REACH`で
間合いと扇を一回り広げ、剣士との差を数値にも出した。

### 6-4. バーサーカーの再簡略化

接線方向の自動周回は「どちらへ動くのか分からない/動かされている感覚」という
実プレイ評価を受けて撤去(`core/flank-step.js`ごと削除)。
移動入力があればその方向へスライド、無ければほぼその場、という形に単純化した。

### 6-5. 鷹の目の限定ターンアシスト

突進を回避 → 背後を抜けられる → 振り向き直す、の往復を減らすため、攻撃入力の
瞬間だけ「予兆行動中の敵が近距離かつ約99度以内」なら向きを合わせる
(`canTurnAssist`/`assistedAimYaw`)。180度自動ターン・遠距離ロックオン・
ホーミング化はしない。向きが決まった後は既存の未来位置予測が乗る。

### 6-6. 空中アクション

- **空中回避を禁止**(`tryDodge`に接地チェック)。空中は「見切り・Enemy Step・
  落下攻撃」のための限定状態にする
- **Enemy Step**(`src/core/enemy-step.js`)を新規実装。突進の実行中
  (雑魚`chargeState==='dash'` / ボス`special==='charge' && specialPhase==='dash'`)の
  敵を、空中から落下しながら踏むと発動。体幹+55、跳ね返って再ジャンプ、
  突進は中断される。1回の滞空につき1度だけ。徘徊中の敵は踏めない。
  無敵もスーパーアーマーも付与しない
- **落下攻撃**は既存の`tryJumpAttack`/`landJumpAttack`をそのまま利用。
  Enemy Stepの跳ね返りからそのまま繋がる

### 6-7. Test Mode拡張

- Arenaパネルから鑑定所(スキル選択+スフィア盤のタブ)を開けるようにした。
  通常は酒場の鍛冶屋の前限定という位置制限を、テストモードでだけ外している
- テストモード開始時に派生スキルを全解放し、スフィアポイントを潤沢に付与。
  さらに`sphereCanUnlock`/`unlockSphereNode`でポイント消費自体を免除
- 隔離は既存の仕組みで足りている: `saveGame()`が`state.testMode`で必ず弾くため、
  テストモード中の変更は通常セーブへ一切書き戻らない(E2Eで検証)

### 6-8. 今回も未実装のまま残した項目(※7-5で実装済み)

- **切り上げ攻撃**: 過去の設計記録が依然として存在しないため、仕様を補完しての
  実装は行わなかった。ただし今回で空中アクションの土台(空中回避の禁止・
  Enemy Step・落下攻撃)が揃ったので、次はこの上に設計できる
  → 第3次(7章)でユーザーから仕様が示されたため、この土台の上に実装した

---

## 7. Combat Feel & Air Action(2026-09、第3次)

方針は前章までと同じ ―― 既存の完成した要素は壊さず、「各職業の操作感」と
「空中アクション」だけを仕上げる。シナリオ・ダンジョン・マップ・イベントには
一切触れていない。

### 7-1. 攻撃タイムラインの同期(戦騎士のSE)

ユーザー報告「攻撃SEが実際の剣振りより早い」の原因は、3つの時刻が別々の
場所で別々に決まっていたこと:

| | 決めていた場所 | 実時間(クリップ長比) |
|---|---|---|
| 攻撃SE | `beginMove()` が再生開始と同時に `moveSfx()` | **0.00** |
| Hit判定 | `JOB_HIT_DELAY_FRAC`(11-combat-actions.js) | 0.45 |
| 見た目の刃 | `JOB_SWING_ANTICIPATION = 3.2` の `t^pow` 歪み | 0.57〜0.60 |

戦騎士は3つすべてが掛かる唯一の職なので、ズレがそのまま違和感になっていた
(`swingDur` = 0.36×テンポ1.25 = 0.45秒 なので、SEは刃の通過より約0.26秒早い)。

`src/core/swing-timing.js` に1本のタイムラインとして集約し、

- **Hit判定の 0.45 は据え置き**(手触りが良いと確認済みのため動かさない)
- 攻撃SEを同じ 0.45 へ移す(`state.pendingMoveSfx`)
- `anticipation` を 3.2 → 2.2 に緩め、刃の通過を 0.45〜0.48 へ引き戻す

とした。結果、`impactClipFrac('battleKnight')` = 0.173 が、袈裟斬りクリップの
刃の通過区間(0.12〜0.22)に入る ―― 3つが同じ瞬間を指すようになった。
2.2 は等速(1.0)とは程遠いので、静→動の重量感は残っている。

他クラス・スキル・回避攻撃・必殺技のSEは1フレームも変わっていない
(`swingSfxDelay()` が 0 を返す)。

### 7-2. バーサーカー: 移動方向と攻撃方向の分離(`src/core/soft-lock.js`)

ユーザー報告「後ろへ下がりながら攻撃したいのに、後ろを向いて攻撃してしまう」。
原因は攻撃方向が `state.facing` そのもので、`state.facing` は移動入力で
回っていたこと。

コンボの1段目で「近距離(6.5)かつ正面101度以内」の敵を1体だけ候補にし、
そのコンボの間だけ `state.facing` をロック方向へ保つ。移動ベクトルには
一切触れていないので、どこへスライドするかは最後までプレイヤーの入力が決める。

- 解除は「コンボが途切れる」「対象が死ぬ/8.5より離れる」の2つだけ。
  **向きでは解除しない** ―― 横や後ろへ入力している最中こそ保ってほしい
  場面なので、そこで切ると要求そのものを壊す
- 取得角度101度は「敵が前、入力は左」で体が90度回った状態を拾える最小限。
  背後(180度)は絶対に拾わない
- 向き直しは 16 rad/s(1フレーム約15度)で、瞬間的な自動旋回にはしない
- カメラ固定・強制ターゲット・自動追尾・自動接近はいずれも無し
- 前章6-4のスライド攻撃(入力方向へ 1.5 / 2.2、無入力ならほぼその場)は
  そのまま維持。自動周回には戻していない

### 7-3. 鷹の目: 先読みが効く範囲の拡張

**本体は角度ではなく分岐順だった。** `tryAttack` で回避攻撃の分岐が
`applyHawkEyeTurnAssist()` より手前にあったため、この職の狙いどおりの流れ
――「突進を回避してすぐ撃つ」―― では補助が一度も走っていなかった。
順序を入れ替え、攻撃の種類を決める前に向きを決めるようにした。

その上で角度を2段構えにした:

- 通常: 99度 → **130度**(側面へ回り込まれた敵を拾う)
- 回避直後(`state.hawkAssistT`、1.1秒): **155度**(抜けられた直後だけ広げる)
- 射程: 14 → 16

155度は「肩越しに振り返って射る」まで。180度には届かせない ―― 背中を
向けたまま撃てるのは完全自動ターンであり、禁止事項そのものだから。
補助の後は既存の未来位置予測(`predictLeadPosition`)がそのまま乗るので、
「現在位置ではなく移動先を撃つ」という職の性質は変わっていない。
矢は直進のまま(ホーミングにはしていない)。

### 7-4. 空中スキルの禁止

空中で発動できた戦闘スキルは、実際には**制限がまったく無かった**。
`blockedInAir()` を1つ用意し、5経路すべてに通した:

| 経路 | 入力 |
|---|---|
| スキル | 専用ボタン / Y / KeyL |
| スキル2 | L2 / KeyO |
| スキル3(ボス技) | D-pad左 / KeyU |
| 必殺技 | R2 / KeyK |
| 溜め技 | 攻撃ボタン長押し |

溜め技だけは「攻撃ボタンのタップ = 空中攻撃の入力」なので塞げない。
そこで**溜めへ昇格させない**形にした ―― 離した時は通常どおり
`tryAttack()` へ落ち、空中攻撃になる。地上で溜め始めてからジャンプした
場合も、空中に入った時点で溜めを打ち切る。

Enemy Step・切り上げ・落下攻撃はこの経路を通らないので、当然そのまま使える。

### 7-5. 切り上げ攻撃(`src/core/uppercut.js`)

**新しいボタンは追加していない。** 攻撃ボタンの意味を垂直速度で読み替える:

```
接地                        → 通常コンボ
上昇中(yVel > 1.2)         → 切り上げ攻撃
頂点付近・落下中・使用済み  → 既存の落下攻撃
```

タッチ操作でもコントローラーでも「ジャンプしてから攻撃を押す」以上のことは
要求しない。頂点付近(速度ほぼ0)を上昇中に含めないのは、そこで押した時に
どちらが出るか読めなくなるため。

- **コンボではない**。通常コンボの4段目に繋がる必須操作にもしていない
- 1回の滞空につき1度だけ(`state.uppercutUsed`、着地で戻る)
- 既存の `spin` クリップを流用し、SEだけ差し替え(`beginMove(name, sfxName)`)
- 無敵・スーパーアーマーは付けない。Enemy Step判定も同時に生きている

**敵への効果**(重量クラスは既存フラグの読み替えのみ。新しい分類表は作らない):

| 相手 | 効果 |
|---|---|
| 軽量(通常のモブ) | 体幹 ×1.8 + 0.85m 浮かせ(0.5秒、その間AI停止) |
| 重量(ボス/中ボス/強敵/盾持ち/砲台) | 体幹 ×1.3 + 短い怯み。**浮かせない** |
| 飛行(`en.flying`) | 飛行を解いて地上へ落とす |

浮きは0.85m ―― 無双系の打ち上げにはしない。目的はダメージではなく
「軽量敵の行動を一瞬乱す」こと。

**飛行敵について**: 本編に飛行状態を持つ敵は存在しない(亡霊系の
`mob.hover` は脚を隠して漂わせる描画フラグで、高度も空中状態も持たない)。
指示どおり新規の敵システムは作らず、`en.flying` を立てて `basePos.y` を
上げるだけで乗れる受け口だけを用意した。敵AIには一切触れていない。
経路を目で確認できるよう、Combat Test Arena に検証用の
`Flying Test`(既存の passive を浮かせただけ)を1体追加している。

### 7-6. 既存Stagger設計との関係(指示17の回帰確認)

切り上げの位置づけ(`staggerMul` 基準):

```
通常攻撃 1.0 < 切り上げ(重) 1.3 < 切り上げ(軽) 1.8
         < Perfect Brace 2.2 < 回避攻撃 2.5 < 落下攻撃 3.0
         < Enemy Step(体幹 +55 の固定値)
```

**ボスを切り上げ連打で崩せないのは、倍率ではなく構造で担保している。**
切り上げは毎回「ジャンプ(スタミナ18)→ 上昇中の短い窓 → 着地」という
往復(滞空 約0.727秒)を要求する:

```
切り上げ連打 10 × 1.3 × 1.3 / 0.727 = 23.2 /秒
通常攻撃     10 × 1.0 × 1.3 / 0.52  = 25.0 /秒
```

連打しても、ただ殴り続けるより遅い。重量側の 1.3 はこの不等式が成り立つ
上限から余裕を取った値で、`tests/unit/uppercut.test.js` が回帰として
固定している(倍率を上げると必ず落ちる)。実際、最初に置いた 1.5 は
26.8/秒 となり、このテストが実装中に捕まえた。

前章6-1の体幹修正(減衰の絶対量化・ボス `postureMax` のHP分離)には
一切触れていない。

### 7-7. 空中で塞いだ抜け穴

`tryAttack` の空中分岐が `!state.grounded && !state.jumpAttacking` だったため、
急降下に入った後の攻撃入力がすり抜けて**地上コンボが空中で振れていた**。
空中は選択肢を絞った状態にするのが設計なので、接地していない間の攻撃入力は
例外なく `tryAirAttack()` が引き受けるようにした。

### 7-8. 今回あえて実装しなかったこと

- **未来位置予測のコーン拡張**(`spawnProjectileSingle` の `fDist*0.5`、
  約±27度)は広げていない。ターンアシストが向きを敵へ合わせた後は
  敵が0度に来るので予測は必ず効く ―― コーン自体を広げると
  「適当に撃っても当たる」側へ倒れるため
- **飛行敵そのもの**は追加していない(7-5参照)
- 職業ごとの切り上げモーション分岐は作っていない。既存クリップの流用と
  SE差し替えに留め、アニメーションシステムには手を入れていない

## 8. 体幹の回復開始遅延(Posture Recovery Delay、2026-09、第4次)

### 8-1. 症状 ― 「意図的な体幹崩し」が成立しなかった

6-1 で減衰を絶対量(通常敵16/秒・ボス12/秒)へ直した後も、実プレイでは
**70%の大怯みを見てから追撃して100%のダウンまで持っていく**という組み立てが
安定しなかった。原因は減衰の速さではなく、**減衰が「当てた次のフレームから」
始まる**こと ―― このゲームが要求する一拍(攻撃の振り、回避、間合いの取り直し、
敵の攻撃をやり過ごす待ち)のあいだ、体幹は常に戻り続けていた。結果、
「敵を見る」ほど体幹は削れず、連打だけが唯一の削り手段になっていた。

### 8-2. 対処 ― 減衰の値は変えず、開始を遅らせる

`src/core/stagger-math.js`:

```
POSTURE_RECOVERY_DELAY_SEC = 1.5
```

**体幹が実際に増えた瞬間から1.5秒間、自然回復を始めない。** 減衰そのもの
(16/12)、体幹倍率、Enemy Stepの+55、クラス性能、ボスAIには一切触れていない。

遅延を1.5秒へ戻すのは「**実際に増えた体幹(actualGain)が正のとき**」だけ:

```js
const actualGain = gainPosture(en, gain);   // 内部で clamp 後の差分を返す
// actualGain > 0 のときだけ en.postureRecoveryDelayT = 1.5
```

上限で頭打ちになった一撃(`55/55` に `+10` → actualGain 0)で遅延を延ばすと、
満タンの敵を殴り続けるだけで永久に減衰を止められてしまうため。

### 8-3. 体幹まわりの正式仕様(現行)

| 項目 | 値 / 挙動 | 実装 |
| --- | --- | --- |
| 体幹最大値(通常敵) | 55(強敵130・ガード持ちは×1.3) | `06-player-enemy.js` |
| 体幹最大値(ボス) | `bossPostureMax()` = 180〜320にclamp(HPインフレと切り離す) | `core/stagger-math.js` |
| 1撃の基本値 | `BASE_STAGGER_GAIN = 10` ×(技倍率 × クラス倍率 × 能力倍率 × パニッシュ倍率) | `staggerGain()` |
| 70%到達 | 大怯み(`bigFlinched`、`hurtT`を0.5秒へ延長+トースト) | `applyStaggerResult()` |
| 100%到達 | ダウン(`triggerKnockdown`、AI停止 2.2〜3.0秒) | `07-ai-combat.js` |
| 自然回復(通常敵) | 16 /秒 | `POSTURE_DECAY_PER_SEC` |
| 自然回復(ボス) | 12 /秒 | `POSTURE_DECAY_PER_SEC_BOSS` |
| **回復開始遅延** | **体幹が実際に増えてから1.5秒は減衰しない。増えるたびに1.5秒へリセット** | `POSTURE_RECOVERY_DELAY_SEC` / `en.postureRecoveryDelayT` |
| 大怯みリアクション中 | 減衰しない(`bigFlinched` かつ `hurtT > 0` の間)。100%への追撃猶予 | `canDecayPosture()` |
| ダウン復帰後 | `posture = 0` / `postureGraceT = 1.5` で**再ダウン防止**。`postureRecoveryDelayT` とは別用途 | `07-ai-combat.js` |

`postureGraceT`(ダウン復帰直後は体幹が溜まらない)と
`postureRecoveryDelayT`(体幹が増えた直後は自然回復しない)は**別の状態**で、
片方をもう片方で置き換えていない。`bigFlinched` も「減衰停止フラグ」ではない
――70%未満へ戻るまで残り続ける再発火防止フラグのままで、減衰を止めるのは
あくまで大怯みリアクション(`hurtT`)が残っている間だけ。

### 8-4. 設計意図 ― 「殴っても崩せる/読めばもっと速く崩せる」

- **普通に攻撃すれば徐々に削れる**: 攻撃間隔(剣士0.52秒)は遅延1.5秒より
  短いので、殴り続けている限り目減りしない。連打の到達点は従来と同じ。
- **読むと明確に速い**: パニッシュ窓(振りかぶり×1.6/振り抜き×1.3)、
  回避攻撃×2.5、落下攻撃×3.0、Perfect Brace×2.2、Enemy Step +55 で
  一度に増やしたぶんが、次の一拍で戻らずそのまま残る。読みの価値が
  「削り量」だけでなく「削った体幹が保持されること」にも乗る。
- **連打だけが最適解にはならない**: 手を止めれば1.5秒後に従来どおりの
  速度で戻る。回避や間合い取りのコストは据え置きで、遅延は「攻撃の合間」を
  許容するだけ。
- **敵ごとの体験**: 通常敵(55)は普通に殴っていれば自然に大怯み→ダウン。
  強敵(130)は殴っても崩せるが、Punish / Dodge Attack / 切り上げ /
  Perfect Brace / Enemy Step / フィニッシュを使うと明確に速い。地下の
  「燭台を提げた影」の チャージ→回避→Enemy Step(+55)→追撃 が、そのまま
  体幹チュートリアルとして機能する。ボスは既存の攻撃パターン・隙・
  位置取りを利用して削る設計のままで、AIには手を入れていない。

### 8-5. 実装箇所(体幹加算は1経路に集約)

- `src/core/stagger-math.js`: `POSTURE_RECOVERY_DELAY_SEC` / `canGainPosture()` /
  `gainPosture()`(加算 + actualGain + 遅延リセット)/ `canDecayPosture()` /
  `stepPostureRecovery()`(遅延の減算 → 減衰 → 70%未満で大怯み再発火を許可)
- `src/legacy/parts/07-ai-combat.js`: `applyStaggerResult()` が `gainPosture()` を
  呼ぶ1本道になった。通常攻撃・コンボ・フィニッシャー・回避攻撃・落下攻撃・
  切り上げ・弓/魔法のprojectile・パニッシュ・Perfect Brace・Enemy Step・
  ボススキルは**すべてこの関数を経由する**ので、遅延の更新漏れが起きない。
  `updateEnemies()` は `stepPostureRecovery(en, dt)` を呼ぶだけ。
- `src/legacy/parts/12-progression-ui.js`: ボススキル「崩しの型」
  (`onFinishHit2`)だけが `en.posture` を直接書いていたので、
  `applyStaggerResult()` 経由へ寄せた(大怯びを発生させない既存挙動は維持)。
- `src/legacy/parts/06-player-enemy.js`: 敵/ボスの生成時に
  `postureRecoveryDelayT: 0` を追加。
- 回帰テスト: `tests/unit/posture-recovery.test.js`(加算・actualGain・遅延の
  更新/リセット/refresh・遅延中は減衰しない・遅延後は16/12・大怯み・ダウン・
  `postureGraceT`・Enemy Step +55)。

新しいUI・ボタン・体幹専用スキル・専用装備は追加していない。既存の
盾の発光(体幹比率で青→橙)、ダウン姿勢、`14-hud-boot.js` の体幹バーが
そのまま可視化を担う(Combat Test Arenaのデバッグ表示にだけ、遅延の
残り秒数を1項目追記した)。オーディオ・ライティング・Static batching・
iPhone向け最適化には一切触れていない。
