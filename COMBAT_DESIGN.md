# 戦闘・職業システム設計ノート

このファイルは、「敵を見ることでプレイヤーが有利になるARPG」という設計思想
(2026-09の設計引き継ぎ指示)に沿って戦闘・職業システムを整理した際の記録。
シナリオ・ダンジョン構造・イベント進行には一切触れていない(それらは別チャットの
担当)。以降の作業はここに追記していく想定。

## 0. 最重要コンセプト(踏襲)

敵を見る → 行動を理解する → 位置を選ぶ → タイミングよく攻撃する →
敵の体勢・向きを崩す → 有利な状況を作る、という基本ループを、単純な
「レベルを上げる/連打する」の上位互換にしない。職業ごとに「何を見るか」を
変える(剣士=距離、戦騎士=身体、盗賊=隙間、バーサーカー=次の位置、
弓師=現在、鷹の目=未来、魔法使い=戦場、魔導士=戦場の変化)。

## 1. 解析結果 - 実装済み・素の状態(2026-09時点)

大量の作業が既に済んでいたため、今回はゼロからの設計ではなく **差分の実装**
になっている。解析で分かったこと:

- **職業構成は既に指示通り**: 基本職(剣士/盗賊/魔法使い/弓師)→上位職
  (戦騎士/バーサーカー/魔導士/鷹の目)の対応、`state.job` フラグ方式、
  上位職専用の見た目・武器モデル(斧/大弓等)・専用スキル1種
  (`unlockKey:'job'`)・専用必殺技(`JOB_ULT_BY_JOB`)まで実装済み
  (`src/legacy/parts/01-character-creation.js`, `06-player-enemy.js`,
  `12-progression-ui.js`)。
- **体幹(Stagger)システムは既にある**: `en.posture`/`en.postureMax`、
  ダウン(`triggerKnockdown`)、大怯み閾値(70%)、クラスごとの
  `staggerMul`、技ごとの`opts.staggerMul`ボーナス(回避攻撃・ジャンプ攻撃・
  各種スキル)まで実装済み(`07-ai-combat.js`)。
- **ジャストドッジ/バリア(パリィ)も既にある**: `state.dodging`中の被弾を
  検出して反撃ダメージ+50%の窓を開く仕組み、および全職業共通の
  「バリア」スキル(構えて弾く+HP吸収、発動中無敵)が既に存在。
- **職業ごとの差別化も基礎はある**: コンボ段数・型がクラスごとに違う
  (大剣3段/双剣4段/杖2段/弓4段)、弓師は近接距離でダメージ-35%、
  盗賊の刀は1段目確定クリティカル、等(`core/damage-math.js`)。

一方で、今回の設計書が明確に「してはいけない」と書いている状態が
そのまま残っていた箇所があった:

- **敵・ボスの向きが毎フレーム瞬間スナップ**
  (`en.group.rotation.y = Math.atan2(...)`)。回転速度の概念が一切なく、
  design #21/#22「瞬時に180度回転してはいけない」に反する。
- **上位職の必殺技/専用スキルが「同じ判定の数値違い」であることが
  コード自体にコメントで明記**されていた
  (`12-progression-ui.js`: 「新しい当たり判定の形は増やさず…数値だけを
  大きく強化してある」)。design #4/#6/#9/#13/#14 が禁止する
  「単純な上位互換」そのもの。
- **攻撃後の「敵の身体の流れ」を利用する仕組みが無い**
  (design #23-26)。ボスは振り終わったら即座に平常運転(=即座に
  プレイヤーへ向き直る)に戻っていた。
- **通常攻撃が完全に足を止めた状態でしか出せない**
  (バーサーカーの「攻撃モーションが移動になる」design #11 が未実装)。
- **鷹の目の「予兆を読んで未来の位置を狙う」が数値上の名前だけ**
  で、実際のロジックは他職の同型スキルと同一だった(design #15-18)。

## 2. 今回実装した差分

既存を壊さないことを最優先し、**新しいボタン/UI/システムを増やさず**、
既存の仕組み(体幹、バリア、スキルのチャージ、`state.skillAnim`の移動演出)
に薄く乗せる形で実装した。

### 2-1. 敵・ボスの向き(`src/core/enemy-facing.js`)

`turnTowardAngle(current, target, maxDelta)` という純粋関数を切り出し
(`tests/unit/enemy-facing.test.js`)、以下のAIの「毎フレーム瞬間スナップ」を
「毎フレーム、旋回速度の分だけ向き直る」に置き換えた:
`updateWanderAI` / `updateFireEnemyAI` / `updateKiteAI` / `updateTurretAI` /
`updateBossAI`(通常の追跡中)。

**変えていない箇所**: 突進(`updateChargerAI`)のダッシュ方向、ジャンプ
(`updateJumperAI`)の空中方向、ボスの振りかぶり中(`atkWindup`)の向き -
これらは「攻撃に踏み切った瞬間、向きが一気に定まる」こと自体が予兆に
なるので、意図的に瞬間ロックのまま残した(design #44完了条件
「攻撃中は必要に応じて方向固定される」)。

旋回速度は雑魚(`DEFAULT_MOB_TURN_RATE`)とボス(`DEFAULT_BOSS_TURN_RATE`、
雑魚の約1/3)で分け、個々の敵に`en.turnRate`があればそちらを優先する
(将来のボス別チューニング用のフック)。

### 2-2. ボスの「攻撃後の身体の流れ」(`updateBossAI`)

ボスの振りかぶりが終わった瞬間(命中・空振り問わず)、`en.postAtkRecoveryT`
(0.45秒)を開始。この間はボスの体がまだ攻撃方向へわずかに流れ続け
(`en.postAtkDriftDir`)、向き直りもこの間だけさらに遅くする。design
#23-26「攻撃後の身体の流れを利用できる」の最小実装。

### 2-3. パニッシュ窓(全職業共通、`src/core/stagger-math.js`)

`dealDamageToEnemy`が体幹ダメージを計算する際、対象が
**振りかぶり中(`en.atkWindup`)** または **攻撃直後の隙
(`en.postAtkRecoveryT>0`)** であれば、体幹ダメージに1.6倍/1.3倍の
ボーナスが乗るようにした(`punishWindowMultiplier`)。特定の技や職業に
紐付けず、**全クラス共通**にしてある - 「敵を見て隙を突く」という設計の
中核ループ自体への見返りだから。命中時は既存のヒットスパークとは別に
専用SEを1つ足し、「今の一撃は違う」と分かるようにした。

### 2-4. 戦騎士: Perfect Brace(`tryPerfectDodge`)

新しい入力は増やさず、既存の全職共通「バリア」スキル(構えて弾く)に
`state.job==='battleKnight'`の場合だけ追加効果を乗せた:

- 攻撃元の敵の体幹を大きく削る(既存のstagger-math.jsをそのまま再利用)。
- `state.braceCounterT`(3秒)の反撃猶予を開き、次の一撃に+40%
  (ジャストドッジの+50%より控えめ、かつクリティカル確定ではない -
  `core/damage-math.js`の`braceCounterOpen`)。

「敵の攻撃を誘う→見る→受け流す→反撃」という design #6/#6-1 の流れを、
既存の何もかもを壊さずに一番小さい差分で成立させてある。単純な
スーパーアーマーは付与していない(構え自体は既存バリアのまま:
発動中は無敵、外すとただの硬直)。

### 2-5. バーサーカー: 攻撃と移動の連結(`tryAttack`)

`state.job==='berserker'`の場合、通常コンボの一振りごとに、入力方向
(押していなければ現在の向き)へ短い踏み込み(`state.skillAnim`、
既存のスキル/溜め技の移動演出をそのまま再利用)を自動で追加した。
フィニッシュはやや長め(2.6)、それ以外は短め(1.7)。ヒット判定自体は
振った瞬間に解決される(既存の仕様のまま)ので、この移動は
「次の一撃のための足運び」専用になる。新しい無敵/スーパーアーマーは
一切付与していない(design #12/#41で明示的に禁止)。

### 2-6. 鷹の目: 予兆を読んだ狙い筋補正(`executeVariant`のline系スキル)

「貫通の一矢」「天賦の一矢」は貫通即着弾(飛翔時間が無い)ため、
矢を誘導する代わりに **狙い筋そのもの** を予兆に応じて補正する形にした
(`src/core/predictive-aim.js`: `telegraphLead`/`predictLeadPosition`)。

`state.job==='hawkEye'`の時だけ、正面のゆるいコーン内に
既存AIが追える予兆状態(突進の踏み込み、跳躍の滞空)の敵がいれば、
**チャージした時間の分だけ**その敵の未来位置を読んで狙い筋を寄せる
(長く引くほど先まで読む = design #18「チャージ中を待ち時間にしない」)。
命中時、対象がまだ予兆状態のままなら体幹ボーナス(`staggerMul:3.0`)と
専用トースト/SEを出す。対象が予兆状態でなければ通常通り(ボーナス無し)。
自動追尾ではない - 元々狙っていない方向の敵は拾わない。

### 2-7. 魔導士: 命中で足取りを鈍らせる(`dealDamageToEnemy`)

「戦場そのものを変える」の最小実装として、`state.job==='archmage'`の
命中1つ1つに`en.arcaneBindT`(2.2秒)を付与し、その間は該当の敵の
旋回速度(`enemy-facing.js`の`resolveTurnRate`が`en.turnRateMul`を参照)と
ボスの追跡速度を0.5倍に落とす。敵側に新しい状態機械を増やさず、
既存の参照点に薄い乗数を足しただけ。

## 3. あえて今回やらなかったこと(将来の拡張候補)

- **鷹の目の必殺技(天翔ける鏃)は据え置き**。現状`radial+sweep`(旋回して
  周囲へ矢を放つ)であり、`aimed`型(魔法使い/魔導士/盗賊/バーサーカーの
  必殺技はこちら)ではないため、2-6の予兆読みはスキル(貫通の一矢/
  天賦の一矢)側に実装した。必殺技自体を`aimed`型へ作り替えるのは
  既存のバランス・演出への影響が大きく、今回は見送った。
- **魔導士の設置型フィールド制御**(design #20の「魔法の設置」「戦場に
  残る効果」)は2-7の一時debuffに留めた。地面に残る持続エリア等は
  範囲が大きいため別途の実装フェーズを推奨。
- **ボスの視線(gaze)を予兆に使う**(design #26)は未着手。既存の首(neck)
  yaw追従はプレイヤーを向く方向にしか使われておらず、「あえて外す」
  演出には新しい状態を要するため見送った。
- **基本4職(剣士/盗賊/魔法使い/弓師)自体の再設計はしていない** - 解析の
  結果、距離依存ダメージ・コンボ型・武器思想は既に design の意図と
  一致していたため、上位職側の差分実装を優先した。
- 雑魚敵(charger/kiteなど)の移動速度自体への`arcaneBindT`適用は
  ボスの追跡速度にしか配線していない。トラッシュ敵にも広げるのは
  小さな追加作業。

## 4. 自動テスト

`npm run test:unit`(node:test)に以下を追加:

- `tests/unit/enemy-facing.test.js` - 角度の正規化・旋回クランプ・
  ボス/雑魚のデフォルト旋回速度。
- `tests/unit/stagger-math.test.js` - パニッシュ窓の倍率、体幹加算の
  クランプ、大怯み/ダウン閾値。
- `tests/unit/predictive-aim.test.js` - 予兆判定、突進/跳躍からの
  未来位置の外挿、遅延(踏み込み中)の扱い。
- `tests/unit/damage-math.test.js`に`braceCounterOpen`のケースを追加。

既存の`npm run test`(Playwright、`tests/save-load.spec.js`の
sortieケース)で、上記変更を含めた状態でダンジョンに入り実際に
攻撃してもconsole error/pageerrorが出ないことを確認済み。
